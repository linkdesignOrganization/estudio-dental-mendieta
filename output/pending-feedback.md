# Pending Feedback (para el Feedback Curator)

> Los agentes appendan su feedback de aprendizaje aquí (header `### Feedback de: {agente}`); el PM agrega feedback del cliente.
> El Feedback Curator lo procesa y distribuye en el paso 4j (Fase 4), 5i (cada iteración de Fase 5) y FASE R5 (Onboard).
> Procesado y truncado por última vez: Fase 4 (Construcción Visual) — clínica dental.

### Feedback de: qa-orchestrator

- **Consolidación de una iteración de AUDITORÍA (no de construcción nueva):** cuando una iteración de FASE 5 solo audita una base ya cubierta por la regresión de FASE 4 y aísla unos pocos gaps, la consolidación NO debe tratar la ausencia de resultados de Edge/Visual como laguna de cobertura. Si el `test-distribution.md` documenta que esos sub-testers no fueron asignados (sus criterios están PASA automatizado vía la suite de FASE 4), el gate de cobertura se cumple con solo los criterios asignados al sub-tester activo + la regresión. Forzar INCOMPLETO por archivos de resultados inexistentes de sub-testers no asignados sería un falso positivo.
- **Un "1 failed" en la regresión no siempre cuenta contra el gate:** antes de marcar HAY_BUGS por un test rojo, verificar si el fallo ya fue diagnosticado como flaky de test (no bug de producto) y estabilizado en un archivo dedicado (aquí `flow-ux022.md`: carrera de navegación del wizard, app correcta, test reescrito con gates web-first, 12 corridas verdes). En ese caso la regresión es efectivamente VERDE y el criterio es PASA (test estabilizado). El gate `0 bugs` se refiere a bugs de PRODUCTO, no a deuda/flakiness de test ya resuelta.
- **Regresión de efecto colateral en el seed:** cambios de presentación que comparten una fuente de aleatoriedad con datos numéricos (un cambio para thumbnails reales consumiendo del mismo PRNG que los importes) pueden romper otros criterios silenciosamente. Vale la pena registrar en el qa-report que la regresión fue causada/corregida (PRNG separado) para que quede trazable; el patrón "separar el PRNG por dominio de datos" evita reincidencia en iteraciones futuras del mismo proyecto.
- **Gap-analysis en MODO PLAN cuando los specs NO se nombran por el ID del criterio:** la suite de este proyecto está anclada en DC/UX/BVC (FASE 4), no en REQ-xxx, así que un grep por `REQ-1\d\d` da 0 y NO prueba ausencia de cobertura. El método correcto es grepear por el **comportamiento** (rutas, copy de empty-state, nombres de control: `pieza`, `afiliado`, `Sin procedimientos`, botón `Filtros`) y luego LEER el match para distinguir cobertura **transitiva** (el test pasa por la pantalla) de cobertura **aseverada** (el test asevera el comportamiento nuevo). Caso típico de esta iteración: un criterio queda PARCIAL — su sub-parte funcional (control editable de pieza, camino vacío de un campo) ya está cubierta, pero la sub-parte nueva del Developer (historial real derivado del store, nombre FDI, camino POBLADO de un campo) NO. Asignar solo la sub-parte sin cubrir, e instruir explícitamente al sub-tester a NO duplicar la ya cubierta, mantiene el plan proporcional y evita specs redundantes.
- **El "camino inverso" de un empty-state ya cubierto es un gap real:** si la suite cubre solo el caso VACÍO de un campo (placeholder cuando no hay dato, p.ej. DC-106 sobre el paciente sin contacto), el caso POBLADO (el dato REAL renderizado) sigue sin aseverar — y suele ser justo lo que el Developer corrigió (reemplazar hardcode por dato real). Conviene asignar el camino poblado en un paciente distinto y exigir que el spec confirme que NO es el placeholder.

### Feedback de: devops
- Al hacer commit de cierre de fase (solo tests + docs, sin código de app), el CI/CD de Azure Static Web Apps se dispara igual pero termina verde en ~2min; conviene esperar a `gh run list` completed antes de retornar para confirmar que el deploy no rompe.
- En zsh, `status` es variable read-only del shell: nunca usarla como nombre de variable en scripts de polling de `gh run list` (rompe el loop con "read-only variable"). Usar nombres como `st`/`run_status`. (Reconfirmado en cierre de Iteración 2: el loop se cayó con `(eval):10: read-only variable: status` en el primer intento.)
- **Para un commit de "cierre de iteración" donde el código YA fue desplegado en el pre-QA deploy (aquí el código de It2 estaba en d390e63), el commit de cierre debe contener CERO código de app — solo `e2e/tests/`, `e2e/evidence/`, `output/iterations/iteration-N/`, `project-tracker.md`, `pending-feedback.md`.** Lo verifiqué con `git status --porcelain | grep -E '^.D|^D'` (sin eliminaciones) y `git add -A --dry-run | grep -Ei 'dist/|node_modules/|.angular/|.env$|.DS_Store'` (sin artefactos prohibidos) ANTES del commit. El CI igual se dispara y redeploya el MISMO bundle (el código no cambió), así que el verde confirma "el deploy sigue sano", no "se desplegó funcionalidad nueva". Útil distinguirlo: si este CI fallara, el problema es del pipeline/infra, no del código de la iteración.
- **Las `gif-frames/` (PNGs intermedios usados para componer los GIFs de evidencia) sí entran al commit junto con los `.gif`/`.png` finales** — son parte de `e2e/evidence/iteration-N/` y no están en `.gitignore`. No son artefactos de build; son evidencia reproducible. No filtrarlos.

### Feedback de: devops (Iteración 1 — Pre-QA Deploy)
- **Verificar un "fix de datos seed" en frontend-only requiere mirar DOS capas distintas, no solo el manifest.** El `manifest.json` solo lleva *assets crudos* descubiertos en build-time (paths de fotos/SVGs); los datos derivados (los 6 profesionales, la asignación doc→paciente) se generan client-side desde `ar-data.ts`, compilados en un **lazy chunk de JS** (no el `main-*.js`, que aquí eran solo 25KB de bootstrap). Para confirmar los nombres exactos hubo que (a) curl al manifest para los 15 paths de SVG, y (b) bajar TODOS los chunks referenciados y greparlos. El `main` casi nunca tiene los datos de feature: con el builder `application` de Angular están code-split. Regla: para verificar datos de un feature en el bundle desplegado, enumerar los `chunk-*.js` (del `index.html` + los que esos chunks referencian) y grepear el set completo, no solo `main`.
- **Los acentos se minifican a escapes `\xNN` en el bundle → `grep -F` de la cadena acentuada literal FALLA.** "Martín"/"Béccar"/"Acuña" aparecían como `Mart\xEDn`/`B\xE9ccar`/`Acu\xF1a`. Di un falso "MISSING" hasta que grepié el stem sin acento (`Aguilera`, `Varela`) o el escape (`\xED`). Regla: al verificar strings con tildes/ñ en un bundle minificado, grepear por el stem ASCII o por el patrón `\xNN`, nunca por el carácter UTF-8 crudo.
- **Para probar que un asset estático sirve de verdad (no SPA-fallback enmascarando un 404) hace falta un CONTROL NEGATIVO, no solo status+content-type.** Confirmé `image/svg+xml` en los 15 SVGs, pero el SWA con SPA fallback devuelve `200 text/html` para CUALQUIER ruta inexistente. Pedí explícitamente un path falso (`/imagenes/documentos/NOPE-99.svg`) → `200 text/html`: eso PRUEBA que el `image/svg+xml` de los reales es genuino. Sin el control negativo, "200 + content-type correcto" no descarta que el server esté inventando la respuesta. (Refuerza la nota previa de infra sobre fotos `/seed/fotos/...`.)
- **"El tab Documentos muestra thumbnails" NO se puede verificar abriendo un paciente cualquiera: por diseño del seed (UX-048) algunos pacientes no tienen documentos.** Abrí `pac-001` y vi "Sin documentos" (placeholder correcto, no bug) — casi lo reporto como fallo. Hubo que iterar `pac-001..029` por su deep-link `/pacientes/:id/documentos` hasta dar con uno con docs (`pac-002`) y ahí confirmar `naturalWidth>0`. Regla: cuando un dato está distribuido parcialmente (N docs sobre M pacientes, "algunos sin"), no asumir que el primer registro lo tiene; iterar hasta encontrar un caso positivo, y reconocer el empty-state controlado como esperado.
- **Verificación de render real de imagen = `img.complete && img.naturalWidth>0` vía Playwright, no el status HTTP del asset.** Un `<img>` puede apuntar a un SVG que sirve 200 pero romperse en el cliente (CSP `img-src`, MIME, parse). Comprobar `naturalWidth>0` en el DOM tras cargar el tab es la prueba de "thumbnail real, no roto" que pidió la tarea; el curl al asset es complementario, no suficiente.

### Feedback de: developer (Iteración 1 — auditoría de Fundación)
- **Auditar una base heredada de la Construcción Visual = correr el smoke test de seed PRIMERO, antes de leer componente por componente.** Bundlear el `seed.generator.ts` con el esbuild local y correr `generateSeed(manifest)` en node revela en segundos qué REQ de volumen/calidad fallan (REQ-056..058) sin abrir un browser. Aquí destapó 2 gaps reales que la demo había dejado "verde a ojo": (1) los 6 profesionales tenían nombres/especialidades/años inventados, no los EXACTOS que pide el requirement de "alta" prioridad; (2) los 44 documentos tenían thumbnail vacío (placeholder controlado) porque no existía el pool de imágenes que el requirement enumera (10 radiografías + 5 fotos). La demo se ve bien pero NO cumple el dato literal — el smoke test es lo que lo caza.
- **Cuando el requirement enumera datos LITERALES (nombres propios, CUIT, listas exactas), el seed debe matchearlos carácter por carácter, no "algo plausible equivalente".** Un generador determinista con datos plausibles parece cumplir pero falla la verificación literal. Grepear los valores exactos del requirement en `src/` confirma rápido si están o no.
- **Cambiar datos seed que los e2e hardcodean rompe tests aunque el cambio sea correcto.** Los flujos de crear-turno seleccionaban profesionales por su label exacto (`{nombre} · {especialidad}`). Antes de renombrar verifiqué con grep las 8 referencias y las actualicé en el mismo cambio (mapeando cada vieja a una nueva de la MISMA especialidad para no alterar la semántica del test). Regla: tras tocar el seed, grepear los valores viejos en `e2e/` y actualizar — si no, el QA reporta "regresión" por un fixture desactualizado.
- **Verificar que un cambio de seed NO mueve OTROS datos de los que dependen tests** porque el PRNG es una secuencia única consumida en orden: `rng.pick(PROFESSIONALS)` consume 1 draw sin importar el contenido del array (longitud 6 igual) → mismo índice → mismo `prof-0X` id, solo cambia el display. Y `buildPatients` corre ANTES que `buildDocuments`. Confirmar con un mini-script (assert nombres esperados presentes) evita romper tests aguas abajo sin darse cuenta. Igual de importante: al refactorizar un loop generador, mantener EXACTAMENTE el mismo número y orden de draws del PRNG, o se rompe el "reset fiel" (determinismo).
- **Para "pool de imágenes placeholder" en hosting estático sin assets reales: generar SVGs deterministas autoalojados** (radiografía gris con siluetas de piezas, foto intraoral rosa+dientes). Se sirven con `img-src 'self'` (no violan CSP, no son data-URI), el generador de manifest los descubre si agregás `svg` al `IMG_RE`, y se copian solos a `dist/` en el build. Codificar el TIPO en el nombre de archivo (`radiografia-NN.svg`/`foto-NN.svg`) permite que el seed asigne thumbnail COHERENTE con el tipo del documento partiendo el pool por prefijo — sin cambiar el shape del manifest (sigue siendo `string[]`).

### Feedback de: plan-verifier
- **5a-verify It1 (Fundación) → PASA, 0 gaps.** Los 2 gaps que el Developer corrigió (REQ-057 nombres exactos de los 6 profesionales, REQ-058 thumbnails reales en 44 docs) quedaron confirmados a nivel de datos.
- **Patrón útil de verificación**: para seeds deterministas frontend-only, en vez de solo leer el código, bundleé el generador con `esbuild --bundle --format=esm` contra el manifest real y corrí `generateSeed()` dos veces → JSON byte-idéntico prueba el determinismo (PRNG semilla fija) empíricamente, y de paso valida TODOS los volúmenes (REQ-058) con conteos reales en vez de inferencia. Mucho más fuerte que muestrear. Recomendable replicarlo en proyectos con seed determinista.
- **Carencia detectada (target = QA)**: el test `UX-060-067-seed.spec.ts` cubre volúmenes pero NO asserta explícitamente (a) determinismo del reset (cambio → Restablecer → estado idéntico) ni (b) los nombres EXACTOS del staff (REQ-057). Como ambos fueron gaps reincidentes, convendría blindarlos en regresión con aserciones directas. No bloqueó 5a porque la verificación de código+seed los cubre.
- **Observación (informativo)**: Configuración tiene 3 switches de "Preferencias" sin REQ asociado — scope creep menor, coherente con presentación de producto, sin demo/tracking. No bloqueante.

### Feedback de: developer (Iteración 1 — Simplify + Security review del diff de Fundación)
- **Para auditar XSS de assets SVG nuevos, lo que decide el veredicto es el SINK de renderizado, no el contenido del SVG.** Un SVG servido por `<img [src]>` corre en contexto-imagen: el navegador desactiva todo scripting (ni `<script>` ni `onload` ejecutan), así que aunque el asset fuera malicioso el riesgo es nulo. El mismo SVG por `[innerHTML]`/`bypassSecurityTrustHtml` SÍ ejecutaría. Aquí confirmé los dos paths: thumbnails de documentos → `<img [src]="d.thumbnailPath">` (seguro), e `icon.component` → `[innerHTML]` pero alimentado SOLO de un allowlist interno (`PHOSPHOR_PATHS`), con el único valor dinámico (`size`) coaccionado a entero acotado, y los SVGs nuevos nunca llegan a ese componente. Regla: grepear `innerHTML|bypassSecurity|DomSanitizer` en `src/` y mapear si el dato NUEVO alcanza alguno de esos sinks; si solo va por `<img src>`/interpolación `{{ }}`, no hay vector aunque el grep "encuentre" un bypass preexistente fuera de scope.
- **Igual escanear los assets es barato y vale la pena como defensa en profundidad:** `grep -RIEli '<script|onload|onerror|onclick|javascript:|foreignObject|<a |xlink:href|<iframe|<use|data:text/html' <dir>` sobre los 15 SVGs (exit 1 = limpios) confirma que son vectores estáticos puros (rect/ellipse/line/text/gradient con `url(#frag)` local inerte). Documentar el exit-code del grep en el RETORNO da evidencia concreta.
- **Un refactor de "simplify" sobre un generador determinista debe preservar el ORDEN y NÚMERO de draws del PRNG, no solo el output de una corrida.** Extraer 3 bloques duplicados de construcción de documento en helpers `thumbFor`/`pushDoc` está bien SOLO si dentro de `pushDoc` la secuencia de `rng` (chance→pick(names)→int(fecha)) queda idéntica a la inline original; si se reordena o se agrega un draw condicional, se rompe el "reset fiel". Verifiqué leyendo el diff que el orden coincide. El `npm audit` (0 vulns) y `ng build --configuration production` (build_exit=0, 102 kB gz, sin budget exceeded) + `tsc --noEmit` (exit 0) son los gates que cierran; las warnings de `@import` de Sass son ruido preexistente aceptable.

### Feedback de: developer (Iteración 1 — corrección regresión seed PRNG)
- **Un draw del PRNG global protegido por un guard de longitud (`pool.length ? rng.pick(pool) : ''`) es una bomba de tiempo de regresión: cuando el pool pasa de vacío a poblado, ACTIVA un draw que antes no ocurría y DESPLAZA toda la secuencia downstream.** En FASE 4 `manifest.documentos` estaba `[]` → ese `rng.pick(pool)` nunca corría (3 draws/doc). En Iteración 1, al poblar los SVGs (REQ-058) el pool dejó de estar vacío → 4 draws/doc → presupuestos/facturas/movimientos cambiaron de valores (saldos, estados) aunque su CÓDIGO no se tocó. Regla: cualquier consumo del PRNG cuya *ejecución* dependa de datos variables (manifest, flags) rompe el determinismo cross-build. Lo cosmético/derivado de assets (thumbnails, avatars) debe usar un **PRNG separado con semilla propia** (`new Prng(SEED_ROOT ^ tag)`), nunca el rng global de la secuencia de negocio.
- **El diagnóstico inicial del PM ("el cambio de buildDocuments/profesionales desplazó la secuencia") era una hipótesis razonable pero FALSA; los datos la refutaron.** Comparar OLD-code vs NEW-code contra el MISMO manifest dio resultados byte-idénticos → el código no era la causa. La causa era el CAMBIO DE MANIFEST (`documentos: [] → 15 SVGs`). Regla: antes de "restaurar el orden de draws" del código, aislar la variable real corriendo el generador con cada combinación {código viejo/nuevo} × {input viejo/nuevo}. El smoke-test en node (esbuild local + `generateSeed`) lo confirma en segundos sin browser; ahí se ve que el verdadero disparador fue el input, no el código.
- **Verificar specs de FASE 4 contra el fix requiere servirlos LOCALMENTE: el `playwright.config.ts` apunta a la URL desplegada (Azure), que aún tiene el build viejo/roto.** Como el deploy lo hace el PM (paso 5d) y yo no debo deployar, serví el `dist` de producción con un mini server SPA-fallback (Node, con control de assets vs fallback a index.html) y un config temporal que extiende el base sobreescribiendo solo `baseURL: http://localhost:4399`. Los 98 tests (49×2 projects) pasaron contra mi build local. Regla: para validar un fix antes del deploy del PM, montar el build localmente + override de baseURL vía config que herede del original; limpiar el config y el server temporales al terminar (no dejar `playwright.local.config.ts` ni `_serve.tmp.mjs` en el repo).

### Feedback de: devops (Iteración 1 — re-deploy fix de regresión de seed, FASE 5)
- **Antes de commitear un redeploy de "solo código", correr el build local pulle como side-effect el manifest tracked (`public/seed/manifest.json`), que cambia SOLO el timestamp `generadoEn` y se cuela en el `git add -A` como churn.** El contenido (29 pacientes / 15 docs) es byte-idéntico; el único delta es la fecha. Lo revertí con `git checkout -- public/seed/manifest.json` antes de stagear, porque el CI lo regenera fresco en cada deploy de todos modos (el prebuild corre en Oryx) → versionar un timestamp distinto en cada push es ruido puro. Regla: si verificás el build localmente antes de commitear y hay un artefacto build-time tracked, revertí su churn de timestamp/hash y dejá que el CI lo regenere; el commit debe contener exactamente el fix + reportes, nada más.
- **El cambio de hash del bundle `main-*.js` en la respuesta del sitio es la prueba barata de que el fix está REALMENTE desplegado y no es caché del CDN.** Tras el push el sitio sirve `main-5CCD4VTD.js` (antes `main-GFYDB2WD.js`); como el contenido del bundle cambió, su hash de contenido cambia → si tras un deploy "success" el hash siguiera igual, sospecharía de un build que no incluyó el cambio o de caché. Verificarlo es un `curl -s "$SITE/" | grep -oE 'main-[A-Z0-9]+\.js'` y comparar contra el deploy previo (documentado en infrastructure-setup.md). Más fuerte que solo mirar `gh run ... success`.
- **Para un redeploy de fix de SEED determinista, el smoke-test de DevOps NO valida la corrección funcional (saldos/secuencia) — eso es de la re-regresión + QA; el alcance de DevOps es "deploy exitoso + sitio carga + manifest correcto".** Confirmé manifest fresco (29+15) por el `generadoEn` posterior al push, content-types genuinos de foto (`image/jpeg`) y SVG (`image/svg+xml`) con control negativo (`NOPE-99.svg` → `text/html` por SPA fallback) para no comer un 404 enmascarado, y headers de seguridad vivos. La verificación de que los presupuestos/saldos volvieron a los de la demo la hace quien corre los specs, no el curl.

### Feedback de: flow-tester (Iteración 1 — Diagnóstico UX-022 flaky)
- **Un fallo "flaky" que NO se reproduce con `-g <test>` aislado casi siempre es una CARRERA dentro del propio flujo, no un bug de la app ni aislamiento de datos.** UX-022 pasaba 6/6 aislado pero falló 1 vez en la suite. La pista decisiva fue el `error-context.md` del artefacto capturado: URL atascada en `/agenda/nuevo/confirmar` con el resumen del wizard VACÍO (`Paciente — Profesional — Fecha y hora — · —`). Regla: antes de teorizar, leer `test-results/<test>/error-context.md` (Playwright lo deja con snapshot del DOM + línea exacta del fallo); el snapshot suele mostrar el estado intermedio que delata la causa raíz. Ojo: ese dir se BORRA en la siguiente corrida `npx playwright test` — copiar/leer el artefacto ANTES de re-ejecutar.
- **Wizards multi-paso con rutas dedicadas + estado en singleton (`providedIn:'root'`) son propensos a carreras: un click "Siguiente"/"Confirmar" puede dispararse antes de que el paso comprometa su dato.** La cura estable es GATEAR cada transición con una aserción web-first que confirme el commit del paso ANTES de avanzar: estado `is-selected` en la opción elegida (paciente, slot) y, sobre todo, que el RESUMEN del paso final NO esté vacío antes de "Confirmar". Estos gates auto-reintentan (expect timeout) y eliminan la ventana de carrera sin `waitForTimeout` arbitrarios.
- **No confundir "estado persistido sucio" con "carrera de UI": Playwright da contexto+localStorage FRESCO por test**, así que un seed re-hidratado por `warmSeed` no arrastra slots ocupados de tests previos dentro de una corrida. Cuando el síntoma es "datos vacíos en un form que se acababa de llenar", sospechar timing de render/navegación, no leakage entre tests.
- **Determinismo de datos en flujos de creación: elegir inputs garantizados-libres (fecha lejana/única + recurso poco usado del seed) en vez de reutilizar fechas/slots que otros tests del mismo archivo tocan.** Cambié la fecha de UX-022 a una lejana (2026-09-30) para que el slot esté libre con cualquier seed y sin colisión con tests vecinos, blindando el test ante cambios futuros del seed.

### Feedback de: qa-orchestrator (Iteración 1 — MODO PLAN, auditoría de Fundación)
- **En una iteración de AUDITORÍA con suite de regresión ya verde, el plan de QA NO es "re-asignar todos los criterios"; es un GAP-ANALYSIS test-por-test: qué corrigió el Developer en ESTA iteración Y qué de eso no tiene aserción dedicada.** La Fundación tenía 480 tests verdes, pero el Developer corrigió 2 datos del seed (REQ-057 nombres/años exactos, REQ-058 thumbnails reales). Asignar las 58+ REQ a sub-testers habría sido desproporcionado y redundante. Asigné SOLO los 2 gaps (ambos al Flow Tester) y marqué el resto PASA (automatizado). Regla: "criterio cubierto por la suite" ≠ "criterio NUEVO/corregido sin aserción dedicada"; solo el segundo grupo va a sub-testers.
- **"Existe un test que toca el criterio" NO implica que el criterio NUEVO esté cubierto: hay que leer QUÉ asserta, no solo que el ID/nombre aparezca.** `UX-063` "cubre" profesionales pero solo asserta `length===6`; `UX-064` "cubre" documentos pero solo asserta `length` 40-60 en localStorage. Ninguno verifica lo que el Developer corrigió (los 6 nombres+especialidad+años EXACTOS, ni el render real del thumbnail en el tab Documentos con `naturalWidth>0`). Un grep de IDs/nombres da falsos positivos de cobertura; abrir el `.spec.ts` y leer las aserciones es lo que destapa el gap real. Cross-checkear con la nota "Notas para QA" del plan-verifier (verification-5a.md) confirmó ambos gaps a nivel de fuente — el verifier suele dejar señaladas exactamente las carencias de regresión.
- **Trampa de asignación heredada del seed: los "no broken image" checks de la suite corren sobre la lista/avatares de pacientes, NO sobre la grilla del tab Documentos.** Casi doy REQ-058/thumbnails por cubierto al ver `naturalWidth===0` checks en `UX-048`/`DC-055`. Pero esos targetean `main img` de `/pacientes` (avatares), no `/pacientes/:id/documentos`. Al instruir al sub-tester precisé el target correcto (la grilla del tab) y el caso-positivo (`pac-002` SÍ tiene docs; `pac-001`/`pac-007` muestran empty-state por diseño — no es bug). Regla: al delegar una aserción de render, especificar la RUTA/elemento exacto, porque un check de imagen-rota en OTRA pantalla no protege el criterio.
- **Detección de ronda por presencia de archivo: sin `qa-report.md` en el dir de la iteración = Ronda 1.** Confirmé con Glob (path absoluto — el patrón relativo da falsos "No files found", documentado en mi MEMORY) que `iteration-1/qa-report.md` no existe → Ronda 1; el campo Ronda del test-distribution lo leerá la próxima ronda para auto-incrementar.

### Feedback de: flow-tester (Iteración 1 — REQ-057/058 seed Fundación)
- **El navegador persistente de Playwright-MCP cachea `localStorage` entre sesiones; el seed solo se regenera con localStorage VACÍO → puede mostrar datos de un deploy ANTERIOR y disparar falsos bugs.** Explorando manualmente, la ficha mostró profesionales fuera del set de 6 ("Dra. Carla Benítez", "Dr. Tomás Herrera"). Antes de reportar, investigué: el browser MCP conservaba un `edm:v1:state` viejo (pre-fix de nombres). El test-runner (contexto efímero) mostraba los 6 nombres correctos y match=true en pac-001..006. Confirmé el producto vía Configuración→"Restablecer datos" → la ficha pasó a "Dr. Federico Salinas" (correcto). Regla de oro: al validar datos del SEED en el browser MCP/persistente, **resetear el estado (o partir de localStorage limpio) ANTES de validar**, y cross-checkear contra una corrida `.spec.ts` (contexto efímero) antes de levantar un bug. No reportar discrepancias de datos del seed observadas SOLO en el browser persistente.
- **Verificar la FORMA real del estado en vivo antes de escribir aserciones evita specs verdes-pero-mentirosos.** Los campos reales eran `professionals[].nombre/especialidad/aniosExperiencia` y `documents[].pacienteId` (no `name`/`patientId`). Un spec escrito a ciegas con `patientId` habría contado 0 docs y, si el assert era `>=0`, habría pasado sin verificar nada. Corrí un `_explore.spec.ts` desechable que dumpeaba `JSON.stringify(state.professionals)`, el primer `documents[0]` y el HTML del tabpanel; con eso fijé selectores/campos exactos. Patrón: explorar→dumpear→escribir aserciones→borrar el explore.
- **Para "thumbnail real, no roto" la aserción robusta es triple: `complete===true` && `naturalWidth>0` && conteo==estado, más el patrón anti-roto `complete && naturalWidth===0 && src` ⇒ 0.** Verificado en desktop Y mobile (ambos projects del config). Derivar el `patientId` del estado (primer paciente con `documents` no vacío) en vez de hardcodear lo hace inmune a cambios de asignación del PRNG entre deploys — relevante porque el thumbnail de pac-002 cambió de `radiografia-06` (infra doc) a `radiografia-01` (deploy actual) sin que eso sea bug.
- **Estabilidad = correr el spec varias veces back-to-back, no una.** 4 corridas completas (24/24 verdes, 0 flaky) antes de declarar PASA. Reusar `warmSeed`/`readState`/`gotoApp` del helper compartido mantuvo el spec alineado con el contrato responsive ya resuelto (sidebar drawer en mobile) y evitó reinventar waits.

### Feedback de: plan-verifier
- **Iteración 1 (Fundación) · 5e-verify · PASA.** 100% de los criterios (REQ-001..058, REQ-247/248/250/251, REQ-264/265/266, REQ-257/258/272, NFR-005..009) con resultado explícito y test automatizado. 0 sin-resultado, 0 bloqueados, 0 sin-test.
- **Headline "480 tests" del qa-report es conservador:** `playwright test --list` reporta **490 tests en 25 archivos** (24 de FASE 4 + `REQ-057-058-seed-fundacion.spec.ts`). El conteo de archivos (25) coincide exacto; el de tests difiere porque varias specs generan tests con `forEach`/loops, no solo `test()` literales. Sugerencia para QA Orchestrator: citar el conteo real de `--list` en el qa-report en vez de un número redondeado, para que la verificación cuadre 1:1.
- **UX-022 correctamente clasificado como flaky estabilizado (no bug):** verifiqué en fuente (`UX-020-032-critical-flows.spec.ts:100-148`) que el test AHORA tiene gates web-first reales — `is-selected` en tarjeta de paciente, `toBeEnabled()`+`is-selected` en slot, y el gate clave de resumen-no-vacío ("Diego Sosa"+"Dr. Juan Pablo Acuña") antes de "Confirmar turno" — más fecha determinista `2026-09-30` y `page.reload()` real. La estabilización ataca exactamente la causa raíz documentada (confirmar con flow vacío). Patrón a reutilizar para wizards multi-ruta con estado en singleton: gatear cada transición, no encadenar clics.
- **Aprendizaje de gap-analysis (bien ejecutado por QA):** los 2 gaps de esta iteración (REQ-057 contenido exacto de profesionales, REQ-058 thumbnails reales) eran sub-aspectos NO cubiertos por specs que sí assertan el "piso" (`UX-063` solo `length===6`; `UX-064` solo conteo de documentos). Confirma que `length`/conteo ≠ corrección de contenido/render. Recomendable que el plan-verifier en 5a marque siempre este tipo de gap "aserción de piso sin aserción de contenido".

### Feedback de: devops (Iteración 1 — commit de cierre 5j)
- **El commit de cierre que SOLO mueve tests + docs igualmente dispara el workflow de SWA y sale verde** (~2min, conclusión `success`). No asumir que "sin código de app ⇒ build saltado": el trigger de Azure SWA es por push a `main`, no por paths, así que SÍ corre y SÍ re-despliega (build idempotente). Hay que esperar `gh run view <id> --jq .conclusion == success` antes de retornar — confirmar contra el SHA exacto de HEAD, no contra `gh run list --limit 1` a secas (puede mostrar el run anterior si el nuevo aún encoló).
- **En zsh, comparar SHAs con prefijo+`*` (`[ "$x" = "ac766db*" ]`) revienta con "no matches found" porque zsh expande el glob ANTES del test.** Usar el SHA completo (`git rev-parse HEAD`) y comparación exacta `[ "$LATEST_SHA" = "$FULLSHA" ]`, o citar/escapar. Evita el falso fallo del loop de polling. (Complementa la nota previa de que `status` es read-only en zsh.)
- **Para enlazar el run con MI push y no con uno ajeno:** poll `gh run list --limit 1 --json headSha --jq '.[0].headSha'` hasta que iguale `git rev-parse HEAD`, recién ahí capturar el `databaseId` y hacer `gh run view <id>` para esperar `completed`. Saltarse el match por SHA puede dar un "verde" que era de un commit anterior.

### Feedback de: developer (Iteración 2 — Pacientes, sub-detalles)

- **El patrón "PRNG separado para datos derivados" se puede llevar más lejos: derivar datos NUEVOS al READ-TIME del store en vez de en `generateSeed`.** El gap real de It2 era el historial de procedimientos por pieza (REQ-186/187), que la demo dejó hardcodeado (2 filas fijas). En vez de agregar una entidad al `StoreState` o un loop en el seed (que habría desplazado el PRNG global y roto los volúmenes/saldos que el e2e asierta exactamente), lo implementé como un SELECTOR del store (`toothHistory(pacienteId, fdi)`) que deriva al vuelo desde (a) el estado actual de la pieza y (b) un `new Prng(SEED_ROOT ^ hash(pacienteId#fdi))`. Ventaja decisiva: como no corre dentro de `generateSeed`, toca el PRNG global CERO veces → el `STATE_SHA256` del seed queda byte-idéntico, garantizado, sin necesidad de "contar draws". Determinismo de reset igual se cumple porque la semilla deriva de claves estables (id+fdi). Regla general: para "datos plausibles que la demo no generó" que NO necesitan persistir (son función pura del estado existente), preferir un selector derivado con PRNG sembrado por clave de entidad antes que tocar el generador del seed.
- **Verificar el matcher de Playwright antes de enriquecer un heading/aria que un test ya asiertaba.** Agregué el nombre FDI al título de la pieza ("Pieza 13" → "Pieza 13 · Incisivo lateral superior derecho"). El test `getByRole('heading', { name: 'Pieza 13' })` NO se rompe porque el `name:` string de Playwright hace match por SUBSTRING (case-insensitive, whitespace-normalizado), no exacto — sigue verde. Pero si hubiera tenido `{ exact: true }` se rompía. Chequear `exact: true`/anclas de regex (`/^.../`) en los specs que tocan la superficie ANTES de cambiar el texto visible evita romper la suite silenciosamente.
- **Cablear un campo de "siempre placeholder" a datos reales puede MEJORAR un test de edge-case en vez de romperlo — si la data del seed coincide.** El tab Info tenía "Contacto de emergencia" hardcodeado a "Sin contacto registrado" (fallaba REQ-128: el seed genera contacto para ~17/29 pacientes). Lo cablée a `patient.contactoEmergencia`. El test DC-106 asiertaba "Sin contacto registrado" para pac-007 (Dante Torres); verifiqué en el smoke-test que pac-007 tiene `contactoEmergencia: null` en el seed → el test SIGUE verde, y ahora por la razón correcta (el dato está genuinamente vacío para ese paciente, no porque esté hardcodeado). Antes de tocar un campo que un test cubre, verificar con un smoke-test del seed que el paciente/registro concreto del test mantiene el valor esperado.
- **El "número de afiliado" (REQ-128) también se deriva determinista sin tocar el seed**: `new Prng(SEED_ROOT ^ hash(id+obraSocialId))` a partir del DNI, particular → "—". Mismo principio que el historial de pieza: dato de presentación plausible, función pura de campos ya persistidos, cero impacto en la secuencia de negocio.

### Feedback de: plan-verifier
**Contexto**: 5a-verify It2 (Pacientes) — modo NORMAL, alcance LECTURA.
- **Patrón que funcionó (anti-drift It2, contraste con It1)**: la verificación de "seed byte-idéntico" se resuelve de forma determinista con `git diff --stat ac766db -- src/app/core/seed/seed.generator.ts` (debe ser vacío) + confirmar que los datos derivados nuevos usan `new Prng(SEED_ROOT ^ hash...)` LOCAL (no el rng de `generateSeed`) + que ningún campo nuevo entra a la interfaz `StoreState`. Los 4 vectores de drift a chequear siempre: (1) generador tocado, (2) PRNG global consumido por datos nuevos, (3) campo nuevo persistido en StoreState, (4) `manifest.json`/`generadoEn` filtrándose al estado. En It2 los 4 dieron negativo → no-drift garantizado sin necesidad de re-correr la regresión completa.
- **Lección de diseño a difundir al Developer**: la técnica "derivar read-time con PRNG separado sembrado por entidad" (toothHistory, numeroAfiliado) es el patrón correcto para AGREGAR datos a la ficha sin romper la regresión del seed. Campos como `numeroAfiliado` se declararon REQUERIDOS en el modelo de presentación `Patient` pero se rellenan en `toPatient` (no en el seed) → no obligan a tocar el generador. Replicar en It3+.
- **Carencia detectada (no bloqueante)**: `contactoEmergencia` es opcional (rng.chance(0.5)) → ~50% de pacientes no tienen contacto. El gap reportado en 5a previo ("hardcodeado→real") era de la capa UI, no del seed; conviene que el QA de 5e cubra explícitamente AMBOS caminos (paciente con contacto y sin contacto → placeholder REQ-130) para no dejar el estado vacío sin aserción.
- **Sugerencia para 5b/5e**: no existen specs para REQ-098..191 todavía. El detalle de pieza (REQ-186/187) necesita una aserción que pruebe coherencia historial↔estado (pieza "sana"→historial vacío; "obturacion"→≥1 procedimiento) para blindar que el PRNG separado es determinista tras "Restablecer datos".

### Feedback de: developer (simplify + security It2)

- **El PRNG separado por dominio (lección It1) se generalizó bien en It2:** los datos derivados al vuelo (`toothHistory`, `afiliadoNumber`) usan `new Prng(SEED_ROOT ^ hashKey(...))` instancias locales, NO el `rng` global de `generateSeed`. Resultado: el manifest/seed quedó byte-idéntico (solo cambió el timestamp `generadoEn`) y el historial/nº-afiliado son deterministas y reproducibles tras "Restablecer datos". Patrón a repetir para cualquier dato presentacional nuevo: derivarlo con un PRNG sembrado por clave estable en vez de almacenarlo o consumir la secuencia de negocio.
- **Una iteración de auditoría simplify+security sobre un DIFF acotado puede cerrar con CERO ediciones, y eso es un resultado válido (no una omisión).** El delta de It2 (sub-detalle de pieza, tab-info, panel de filtros) ya venía limpio: funciones <20 líneas, sin imports muertos (verificado con grep de cada símbolo), sin duplicación (el `estadoLabel` reusa el `STATE_META` canónico del odontograma en vez de duplicar la tabla de labels). Forzar refactors cosméticos sobre código ya correcto sería churn. La auto-verificación (grep de usos + build) es la evidencia de que no había nada que simplificar.
- **Seguridad de datos renderizados en un viewcase frontend-only: el riesgo real es el sink, no el origen.** Todo dato nuevo (historial de pieza, contacto de emergencia, nº afiliado) entra por interpolación `{{ }}` auto-escapada; los únicos bindings de atributo son sobre un enum tipado (`[attr.data-state]`) y un booleano, no sobre texto libre. Como no hay canal de input externo hacia los signals de filtro (son `<select>` con opciones whitelisted usadas solo en comparaciones de igualdad), NO se añadió validación defensiva especulativa: un valor fuera de whitelist solo produciría una lista vacía, nunca comportamiento inseguro. Añadir guards contra inputs imposibles habría sido dead code contra los quality gates.

### Feedback de: devops (Iteración 2 — Pacientes, paso 5d Pre-QA Deploy)

- **El staged-set check confirmó que el patrón "PRNG separado, sin drift" se manifiesta en git como un único cambio benigno en `public/seed/manifest.json`: SOLO la línea `generadoEn` (timestamp).** Las listas de `pacientes`/`documentos` quedaron byte-idénticas (verificado con `git diff public/seed/manifest.json` → un solo `-/+` en la fecha). Para futuros Pre-QA deploys: si el commit declara "seed byte-idéntico" pero el manifest aparece modificado, NO es un red flag por sí mismo — abrir el diff y confirmar que el único cambio es `generadoEn`. Si cambian paths/conteos, ENTONCES sí hay que cuestionar la afirmación de no-drift antes de pushear.
- **Verificación funcional de un deploy: alinear los selectores del probe con el markup REAL antes de declarar FAIL — un selector adivinado produce un falso negativo que parece un bug de feature.** Mi primer script de Playwright marcó FAIL el historial de pieza porque adiviné `.td-list`/`<a href*="/pieza/">`, pero el markup real es `<ul class="td-history"><li class="td-history__item">` y el odontograma navega por evento `(select)`→`router.navigate` (NO hay anchors de pieza). El feature funcionaba perfectamente (pac-003 pieza 18 → 3 procedimientos). Regla: para verificar una feature nueva en el sitio desplegado, hacer `grep` del template del componente para los nombres de clase/estructura EXACTOS (y cómo se navega: routerLink vs. evento programático) ANTES de escribir las aserciones del probe. Un FAIL de verificación debe disparar "¿mi selector es correcto?" antes que "¿el deploy está roto?".
- **Descubrir datos de prueba por enumeración de IDs estables es más robusto que depender de anchors en la página.** Como el odontograma no expone anchors de pieza, enumerar el set FDI canónico (11-18, 21-28, 31-41, 41-48) y visitar `/{id}/pieza/{fdi}` directamente encontró de inmediato una pieza con historial. Para verificar features data-dependientes (que dependen de qué paciente/pieza tiene datos), preferir enumerar el espacio de rutas determinista del seed sobre raspar enlaces de la UI, que pueden no existir según el patrón de navegación.
- **El probe de verificación se ejecuta desde DENTRO del proyecto (no /tmp) e importando `@playwright/test` (no `playwright`).** La resolución de módulos de Node necesita el `node_modules` del proyecto; un `.mjs` en `/tmp` con `import ... from 'playwright'` falla con ERR_MODULE_NOT_FOUND. Copiar el script a la raíz del repo como `*.tmp.mjs`, importar `{ chromium } from '@playwright/test'`, correrlo, y `rm` el archivo al terminar (verificar con `git status --porcelain` que no quedó stray en el árbol antes del commit — aunque en 5d el commit ya ocurrió antes de la verificación, conviene no dejar basura).

### Feedback de: flow-tester

- **Probar "dato real derivado del store" sin caer en tautología:** para un historial/listado que el Developer cambió de hardcode a derivado del estado, no basta con aseverar que "hay texto". La prueba honesta y resistente es el **contraste**: navegar dos entidades en estados distintos (p.ej. una pieza en `obturacion` vs otra `en-tratamiento` del mismo paciente) y aseverar que sus contenidos DIFIEREN y que cada uno es coherente con su estado. Eso descarta un bloque fijo idéntico. En esta iteración (REQ-186) la pieza 18 (obturación → "Obturación con resina compuesta"…) y la pieza 28 (en tratamiento → "Sesión de tratamiento de conducto"…) dieron historiales distintos: prueba sólida de derivación. Complemento barato: aseverar que la atribución (profesional) del historial coincide con el profesional de cabecera que muestra la cabecera de la ficha.
- **Seed determinista por PRNG = strings exactos asertables:** el seed de este proyecto es PRNG-determinista y byte-idéntico entre corridas (verificado: número de afiliado `5653 1461 3760`, fechas `25/03/2026`, descripciones de procedimiento — todos estables en 2 corridas). Por eso conviene aseverar el **valor exacto** (`toHaveText('Soledad Álvarez (Hijo/a) · +54 11 5309-1236')`) en vez de solo un regex laxo; falla más fuerte ante regresiones reales. Riesgo a vigilar: si en una iteración futura el seed cambia (nuevo dato, cambio de PRNG), estos valores exactos deberán actualizarse — están documentados en el spec para facilitarlo.
- **Colisión de strict-mode con el tabpanel:** en esta app el `<section role="tabpanel">` envuelve las cards, y varias cards son también `<section>` con un heading. Un `page.locator('section').filter({ has: heading })` matchea el tabpanel + la card → strict-mode violation (sale en mobile primero por el orden de evaluación). Acotar a la clase real de la card (`section.info-card`) lo resuelve de una. Patrón general: cuando un contenedor de rol (tabpanel/dialog) comparte el tag con el elemento buscado, filtrar por clase/estructura, no solo por tag+heading.
- **Convivencia con specs que MUTAN el store en la misma sesión:** la suite de flujo corre serial con un único `localStorage` compartido y algunos tests escriben (UX-028 muta el estado de una pieza de pac-004; UX-084 registra un pago en pac-008). Elegir entidades distintas para los asserts (aquí pac-001/pac-003, no mutados) evita cross-contamination. Verificado: las 3 specs de flujo relacionadas corren juntas (56 passed) sin interferencia. Recomendación para el sub-tester: antes de fijar un paciente/pieza para asserts de solo-lectura, comprobar que ninguna spec previa de la misma carpeta lo muta.
- **GIF de flujo con ffmpeg (sin ImageMagick):** capturar frames de viewport vía screenshots de Playwright y ensamblar con `ffmpeg -f concat` (lista con `duration` por frame) + `palettegen/paletteuse` da un GIF nítido y liviano (~170KB para 4 frames a 1000px). OJO: el demuxer `concat` resuelve las rutas **relativas al archivo de lista**, no al cwd — poner los nombres de archivo relativos a la carpeta del `.txt`, o falla con "Impossible to open". Repetir el último frame al final del concat evita el corte abrupto del loop.

### Feedback de: qa-orchestrator (Iteración 2 — MODO CONSOLIDACIÓN, auditoría de Pacientes)

- **Consolidar una iteración de auditoría proporcional con 0 bugs se cierra LISTO_PARA_DEMO en Ronda 1 — sin "ciclo de seguridad" innecesario.** Los 4 gaps (REQ-186/128/187/101) PASARON en desktop+mobile con `.spec.ts` dedicado, la regresión dio 486/0 sin drift, y el resto de la ficha queda PASA (automatizado). El gate (0 fallos + 0 bloqueados + 0 regresiones + 100% cubiertos + 100% con test) se cumple en una sola ronda; no hay nada que re-verificar ni motivo para forzar una Ronda 2.
- **La AUSENCIA del Visual Checker NO es un sub-tester faltante cuando el test-distribution lo justifica.** El plan documentó que los cambios de It2 son de datos/contenido/funcionalidad (historial real, nombre FDI, contacto/afiliado, panel de filtros), sin tokens ni superficies responsive nuevas, y que lo visual ya está verde en la suite `DC-*`/`BVC-*` de FASE 4. Por eso la auditoría de cobertura solo cruza los criterios ASIGNADOS (Flow: REQ-186/128; Edge: REQ-187/101) contra los reportados — los 4/4 con resultado. Marcar INCOMPLETO por no encontrar `visual-checker-results-*.md` sería un falso positivo: el responsive puntual (panel de filtros mobile ≥44px, empty-state de pieza) lo cubrió el Edge Case Tester por instrucción de breakpoints, sin duplicar un sub-tester completo.
- **Verificar artefactos con Glob de path ABSOLUTO sigue siendo obligatorio (ver MEMORY).** Confirmé los 3 `.spec.ts` (1 flow + 2 edge-case) y los 4 GIFs con patrones absolutos `**/e2e/tests/...`; el patrón relativo da "No files found" aunque existan. Los nombres de archivo de resultados de esta iteración NO siguieron el sufijo `-ronda-N` del template (fueron `flow-results-it2.md`/`edge-results-it2.md`, indicados por el PM en la invocación) — leer los archivos que el PM nombra explícitamente, no asumir ciegamente el patrón del template.
- **Las "Observaciones (NO son bugs)" de los sub-testers deben quedar TRAZADAS en el qa-report sin contar contra el gate.** OBS-1 (40px desktop vs 44px mobile = contrato mobile cumplido, control compacto desktop válido) y OBS-2 (odontograma `@defer`, ligado a BUG-E04 ya documentado en iteraciones previas, evitado por el flujo de usuario via tab) son decisiones de diseño/comportamiento heredado, no defectos nuevos. Registrarlas como observaciones con su justificación evita que una ronda futura las re-levante como bug; suprimirlas perdería la trazabilidad.

### Feedback de: edge-case-tester (Iteración 2 — Pacientes, Ronda 1)
- **El listado de 29 pacientes pagina (12/pág, 3 págs): `openPatient(nombre)` SOLO clickea filas VISIBLES de la pág. 1.** Para abrir por flujo de usuario a un paciente que NO está en la primera página (p.ej. pac-029 "Cristina Flores", pág. 3), el camino real y determinista es **tipear el nombre en el searchbox "Buscar pacientes" primero** (narra a 1 resultado, "1 pacientes en el sistema"), y RECIÉN ahí `openPatient`. Asumir que `openPatient` pagina sola hace que el test caiga en timeout de 15s sin razón aparente. Vale la pena un helper `findAndOpenPatient(page, name)` que busque y luego abra.
- **El touch target ≥44px es contrato MOBILE, no desktop.** En esta UI los controles del panel de Filtros (botón "Filtros" + comboboxes) miden 40px en desktop y 44px en mobile — patrón responsive correcto, NO un bug. Aseverar ≥44px en el project desktop produce un FALLO falso. Gatear la aserción de tamaño con `testInfo.project.name === 'mobile-chromium'`; en desktop verificar solo que el control sea usable (visible + funcional). Medir con `boundingBox()` por elemento, no asumir un valor global.
- **El diagrama del odontograma es `@defer`: hidrata al entrar en viewport/idle.** Tras navegar al tab Odontograma, los 32 botones de pieza NO existen inmediatamente; hay que `scrollIntoViewIfNeeded()` sobre la pieza objetivo y esperar `getByRole('button', {name:/^Pieza \d+/}).toHaveCount(32)` ANTES de clickear. El deep-link directo a `/pacientes/{id}/pieza/{fdi}` SÍ hidrata el DETALLE de pieza en ambos viewports (no depende del diagrama), así que para aseverar el empty-state de una pieza conviene deep-linkear al detalle; reservar el flujo tab→click para 1 test de camino de usuario. (Relacionado con BUG-E04: deep-link directo a `/odontograma` en MOBILE no hidrata el diagrama — evitarlo.)
- **El aria-label de cada pieza codifica el estado: `"Pieza NN (universal X), <Estado>"`.** Esto permite elegir piezas deterministas por estado sin adivinar (Sana/Ausente/Obturación/Prótesis/En tratamiento). Para el empty-state de pieza, una pieza con aria-label `…, Sana` o `…, Ausente` es candidata; para el control positivo, una `…, Obturación`/`…, Prótesis`. El copy del estado vacío es `Sin procedimientos registrados en esta pieza.` y vive en el `<aside>` con heading "Historial de la pieza".
- **El panel de Filtros expone un badge con el nº de filtros activos en el accessible name del botón:** "Filtros" → "Filtros 1" → "Filtros 2". Para localizar el botón de forma estable a través de los cambios de badge, usar `getByRole('button',{name:/^Filtros\b/})` y aseverar el conteo con `toHaveAccessibleName(/Filtros\s*N/)`. El `group "Filtros de pacientes"` solo existe en el DOM cuando el panel está abierto (`aria-expanded=true`), lo que facilita aseverar el anti-patrón "no visible por defecto" con `toHaveCount(0)`.
- **Combinaciones de filtros sin match deterministas (seed clínica dental):** `Obra social=OSDE + Rango=Adultos mayores (65+)` = 0 · `Obra social=Particular + Rango=Niños (0–12)` = 0. Sirven para forzar el empty-state de la lista filtrada ("No encontramos pacientes con ese criterio" + guidance) sin depender de un profesional concreto.

### Feedback de: plan-verifier
**Contexto**: Post-QA Iteración 2 (paso 5e-verify), modo NORMAL. Verdict: PASA.
- **Cobertura confirmada exhaustiva**: 81 criterios in-scope (REQ-098..172 + REQ-186..191), todos con status QA explícito. 4 con `.spec.ts` dedicado (REQ-186/128/187/101, archivos verificados en disco), 77 PASA (automatizado) por regresión verde (exit 0, 486 passed, sin drift). 0 BLOQUEADOS / 0 FALLA / 0 sin-resultado.
- **Nuance no bloqueante para el QA Orchestrator (etiqueta de bucket imprecisa)**: el `qa-report.md` (línea 46) y `test-distribution.md` (línea 81) rotulan el bucket de regresión como "REQ-173..191". REQ-173..185 son de **Iteración 4** (crear/editar paciente, architecture.md líneas 347-348), NO de Iteración 2. La sobre-extensión es cosmética (los REQ in-scope de sub-detalles 186..191 SÍ están cubiertos), pero conviene rotular buckets de regresión usando los rangos exactos de la iteración para evitar ambigüedad de scope en futuras auditorías. Etiqueta sugerida: "REQ-186..191 (sub-detalles)" en lugar de "REQ-173..191".
- **Patrón sólido a mantener**: cuando los sub-detalles (REQ-189/190/191, Pantallas F/G) quedan como PASA (automatizado) por regresión transitiva (anclada en DEMO-018 route-navigability + DC-045), conviene que el verifier corrobore la implementación real del componente (no solo el "no-crash" del shell-test). En este caso event-detail/plan-detail están plenamente implementados (etapas is-done, costo, progreso, back-links) y el 5a ya los había mapeado a código — la cadena de verificación build→QA→verify quedó consistente.

### Feedback de: developer
**Contexto**: FASE 5 Iteración 3 (Agenda), modo NORMAL. Auditoría + completar a producción sobre la base de la demo.
- **Lección reforzada (presentation-shape vs entity sin tocar el seed)**: cuando una pantalla nueva necesita un campo del dominio que el seed YA genera (acá `notas`, `profesionalId`, `tipoTratamientoId` en `AppointmentEntity`), agregarlo SOLO al shape de presentación (`Appointment`) y mapearlo en el selector `toAppointment`. Cero cambios en `seed.generator.ts` → seed byte-idéntico (verificado: smoke transpila el generador con el esbuild local y asierta `JSON.stringify(a)===JSON.stringify(b)`, 56 turnos, 29 pacientes). Es la forma de "agregar datos a una vista" sin arriesgar el drift del PRNG que rompió tests en It1-2.
- **Patrón nuevo y reutilizable — default de vista responsivo dentro del MISMO componente/ruta**: el feedback pedía que en mobile la agenda arranque en lista cómoda (≥44px) y no en el grid de chips de 22px, pero manteniendo `/agenda` y los toggles. La solución que NO rompe los tests de routing (UX-005/006 corren en desktop Y mobile) es: query param `?vista` opcional + un `effectiveView = computed(() => view() ?? (isMobile() ? 'agenda' : 'mes'))` con un `matchMedia` reactivo (signal + listener limpiado en `DestroyRef.onDestroy`). El grupo de toggles y los bloques-link (con `aria-label` que incluye el estado) quedan SIEMPRE presentes, así los selectores `getByRole('group','Vista del calendario')` y `getByRole('link', /Confirmado|.../)` siguen resolviendo en ambos viewports. Default desktop sin tocar (mes).
- **Budget anyComponentStyle (4kB warn / 8kB error) como señal de cohesión**: al agregar 3 vistas (mes/semana/agenda) a un solo componente, sus estilos inline pasaron de 4kB a ~6.8kB → warning de presupuesto en el prod build. La cura idiomática y limpia fue extraer 2 sub-componentes presentacionales (`week-view`, `agenda-list-view`) con sus propios estilos <4kB cada uno, en vez de subir el budget en angular.json (eso es del Architect) o aceptar el warning. Regla: un warning de estilo-por-componente = el componente hace demasiado; partilo. Build final 103KB gz, 0 warnings (solo el ruido aceptable de `@import` Sass).
- **Notificación "real" sin lenguaje de simulación**: para que el aviso de WhatsApp del reagendado sea genuino (REQ-089) y no solo un toast, `reagendarTurno` ahora persiste una `NotificationEntity` real (id por timestamp, NO PRNG) que aparece en la campana. Los tests de notificaciones (DC-133) ya estaban escritos sin acoplarse a un conteo fijo del seed ("asertamos invariantes: dots==unread, header==unread"), así que sumar una notificación post-acción no los rompe — buena práctica del Flow Tester que conviene seguir exigiendo.

### Feedback de: plan-verifier (It3 Agenda 5a)

- **Drift de seed: el vector más rápido de descartar es `git status --short` + `git log -- <seed>`.** En este proyecto el patrón establecido (It1/It2) es que el Developer separe la aleatoriedad. En It3 lo confirmé en 3 minutos: (1) el generador NO aparece en `git status` y su último commit sigue siendo el de It1; (2) los campos "nuevos" (notas/profesionalId/tipoTratamientoId) ya existían en `AppointmentEntity` desde It1 y el diff de It3 solo los agrega al shape de PRESENTACIÓN + al mapeo `toAppointment`; (3) las escrituras nuevas (`crearTurno`, notif de `reagendarTurno`) usan `Date.now().toString(36)` como id, no `rng.*`; (4) `clinic.version` sigue en '1.0' (sin reseed forzado). Registrar estos 4 vectores explícitamente en el reporte deja trazable POR QUÉ el STATE es byte-idéntico, no solo que lo es.
- **Distinguir "shape de presentación" vs "entidad de dominio" es decisivo para el veredicto de drift.** Agregar campos a la interfaz `Appointment` (presentación) NO toca el estado serializado `StoreState` (que almacena `AppointmentEntity`). Verificar SIEMPRE de qué lado del mapeo `to*()` está el campo nuevo antes de alarmarse: si se rellena read-time desde una entidad ya seedeada (o con `?? ''` defensivo), no hay drift posible.
- **Un literal de requirement puede quedar "sin cobertura" pero ser un GAP de diseño YA resuelto upstream, no un gap de implementación.** REQ-079 pide "u ofrece la opción de crear un paciente nuevo" en el wizard; el código no lo tiene. Antes de bloquear, grepeé el ux-criteria: UX-020 (contrato autoritativo del demo) define el Paso 1 sólo como combo+validación, y el cruce de flujos está explícitamente diferido en GAP-UX02. Como el DEMO-015 fue aprobado así en FASE 4 y está fuera del alcance de It3, lo reporté como observación trazable (no como FALLA). Lección: cuando un literal de REQ no aparece en el código, cruzar contra ux-criteria/GAP-UX antes de decidir origen; un gap aprobado en demo no debe re-bloquear una iteración que no lo toca.
- **Verificación de feedback de demo con doble check (lógica + token):** para "default mobile en lista con touch ≥44px" no basta ver el branch `isMobile() ? 'agenda' : 'mes'`; hay que confirmar que la fila (`.agenda-row`) usa `min-height: var(--touch-target-min)` Y que ese token vale 44px (tokens.scss). El bug original era de 22px (chip del grid), así que el arreglo solo cuenta si el COMPONENTE que se vuelve default es el de filas cómodas, no el grid con el toggle cambiado.
- **`tsc --noEmit` sobre el proyecto app es un check barato y de alto valor en 5a:** confirma que los campos nuevos del shape compilan contra todos los consumidores. Nota de entorno macOS: `timeout` no está disponible en el shell (zsh sin coreutils GNU); correr `npx tsc -p tsconfig.app.json --noEmit` directo (run finito) en vez de envolverlo en `timeout`.

### Feedback de: developer

- **Simplify de "vistas que comparten bloque coloreado" → extraer un sub-componente presentacional, NO un mixin de CSS:** la Iteración 3 agregó dos sub-vistas de calendario (week-view, agenda-list-view) junto al grid mensual ya existente. Mes y Semana duplicaban el MISMO bloque de turno (link + aria-label + `[attr.data-tone]` + helper `badge()`) y las MISMAS 5 reglas de tono (`data-tone="success|warning|info|neutral|error"` → tokens `--color-*-bg/text`), que además ya existían canónicamente en `status-badge.component.ts`. El fix de menor riesgo y byte-idéntico en DOM/CSS es un `CalApptBlockComponent` con un input `layout: 'inline'|'stacked'` que encapsula markup + tonos + el `badge()`; las dos vistas pasan a `<app-cal-appt-block [appt]="a" layout="...">`. La lista usa `app-status-badge` (ya compartido) → no necesita el bloque. Resultado: -1 helper duplicado, -2 copias de markup, -10 reglas CSS de tono, sin tocar el render. Lección: cuando el toggle de vistas duplica una "celda" visual, la unidad de dedup correcta es el átomo presentacional, no una clase utilitaria global (mantiene la encapsulación de estilos de Angular).
- **CSS muerto al mover un átomo a un sub-componente:** al extraer el bloque, la regla mobile `@media { .cal__appt-name { display: none } }` del componente padre queda apuntando a una clase que ya NO está en su template (se fue al sub-componente). Hay que mover esa regla DENTRO del átomo (su propio `@media`) y borrarla del padre; si no, queda como CSS muerto silencioso (no rompe build pero confunde). Verificar siempre con grep de la clase movida en el padre tras el refactor.
- **Import/método huérfano tras dedup:** removido el último uso de `badge()`/`appointmentBadge` en `calendar.component.ts` y de `RouterLink`/`appointmentBadge` en `week-view.component.ts`. Estos no los detecta el build de Angular por sí solo (un método `protected` no usado en template compila igual), pero `tsc --noEmit -p tsconfig.app.json` con `noUnusedLocals` sí marca los imports. Conviene correr el type-check ANTES del build de producción: feedback en ~2s vs ~minuto, y caza imports muertos que el bundler no reporta.
- **Security de la notificación de WhatsApp (reagendar) — sin vector nuevo:** el `subtitulo` de la `NotificationEntity` interpola nombre de paciente + fecha en un string TS que la campana renderiza con `{{ }}` (auto-escapado). No hay `innerHTML` ni `bypassSecurity` en ese camino. El único `bypassSecurityTrustHtml` del repo es `icon.component.ts` (FASE 4, ya auditado): su `name` es clave de un allowlist (`PHOSPHOR_PATHS`, fallback `question`), nunca se interpola crudo, y el único valor interpolado (`size`) se coacciona a entero acotado. Los íconos nuevos de It3 (`chat-circle-dots`, `clipboard-text`) son literales estáticos. Patrón a recordar: datos de dominio (notas, nombre, notificación) que terminan en `{{ }}` están a salvo aunque contengan HTML; solo vigilar `[innerHTML]`/sanitizer-bypass, y ahí confirmar que el dato de usuario no entra al markup.
- **matchMedia con cleanup correcto:** el listener `change` de `matchMedia('(max-width:767px)')` en `calendar.component.ts` registra su `removeEventListener` vía `this.destroyRef.onDestroy(...)` → sin memory leak. Patrón válido para "default de vista responsivo reactivo": `signal` inicializado con `.matches` + listener que hace `.set(e.matches)` + cleanup por `DestroyRef`. No requiere `HostListener('window:resize')` (más ruidoso).
