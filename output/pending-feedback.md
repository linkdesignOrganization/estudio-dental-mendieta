# Pending Feedback (para el Feedback Curator)

### Feedback de: business-analyst

**Patrón detectado — contradicciones residuales en specs versionadas**: El requirements (versión 2) mantenía un dato de la versión 1 ("treinta pacientes" en §12) que contradecía las secciones operativas posteriores (§8/§11: lectura dinámica). Lección: cuando un documento declara su versión y tiene secciones explícitas y enfáticas ("NO usar X como número fijo"), esas ganan sobre menciones residuales en secciones de resumen/restricciones. Verifiqué el dato real (conté 29 fotos) antes de resolver, en vez de confiar en cualquiera de los dos números del documento. Útil para futuros viewcases que también traen specs evolucionadas.

**Patrón detectado — el filtro funcional vs visual fue crítico aquí**: Este proyecto trae un requirements con una §10 enorme de UI (paletas, tipografía, spacing, tokens) y un design-brief dedicado. Apliqué el filtro de rol: extraje los anti-patrones ESTRUCTURALES como criterios (drawers, modales >50%, tablas >5 columnas, tabs como navegación) porque son verificables funcionalmente y son la corrección explícita al error del viewcase anterior, pero dejé fuera paletas/tipografía/spacing (pipeline de diseño). Documenté esa frontera explícitamente en el requirements para que no haya solape con el Design Orchestrator. Sugerencia: mantener esta distinción "anti-patrón estructural = criterio del BA / token visual = criterio de diseño" como regla estable.

**Carencia del documento — alcance de escritura interactiva ambiguo**: El doc describe los tabs (Pagos, Odontograma) y módulos como vistas, sin aclarar qué es editable por el usuario vs solo refleja el seed. En un "demo interactivo" esto es decisión de negocio de alto impacto (define cuánto puede "hacer" el lead). Lo marqué como GAP-B08. Lección para viewcases: siempre preguntar explícitamente el límite lectura/escritura por módulo, porque "demo interactivo" no implica que todo sea editable.

**Observación de proceso**: Primera ejecución del BA en esta instalación (MEMORY.md no existía). Creé los archivos base de memoria (7 dimensiones, clasificación de gaps, checklists por componente, contexto de viewcases). Si el Feedback Curator tiene criterios base canónicos para sembrar el MEMORY del BA, convendría consolidarlos para no divergir entre instalaciones.

**Patrón detectado (Fase 2) — no todo "gap de negocio" es una pregunta para el cliente**: En Fase 1 marqué el cronograma e hitos (GAP-B06) como gap de negocio, pero al clasificar para Fase 2 lo reclasifiqué a asunto de proceso del PM: el cliente no elige entre "opciones de cronograma", el cronograma se deriva del alcance. Lección estable: antes de relayar una pregunta al cliente, verificar que sea una DECISIÓN DE PRODUCTO con opciones reales que el cliente pueda elegir; si es planificación/gestión, va al PM, no al cliente. Esto evita ensuciar la conversación con el cliente con ruido de proceso.

**Patrón detectado (Fase 2 paso 3) — una respuesta del cliente puede ser un CAMBIO DE ALCANCE, no solo elegir una opción**: La pregunta del badge (GAP-B05) ofrecía 3 variantes de marca; el cliente respondió "ninguna: que NO se vea como demo en ningún lado". Eso obligó a barrer TODO el documento (header, footer, Configuración, toasts, notificación de WhatsApp, descripciones de features) y reformular ~8 criterios + agregar una red de seguridad transversal de QA (REQ-272). Lección: cuando el cliente responde fuera de las opciones dadas y eso toca una transversal (presentación/branding), no basta editar el criterio puntual; hay que rastrear todas las apariciones del concepto y dejar un criterio-red para que QA lo verifique de punta a punta. Un grep del documento por las palabras prohibidas ("demo/mock/simulado/placeholder") fue la herramienta clave para no dejar fugas.

**Patrón detectado (Fase 2 paso 3) — dos respuestas SUBIERON el alcance vs la recomendación del equipo (mobile total + ambos tabs editables)**: El equipo recomendó "mobile solo consulta" y "solo Pagos editable"; el cliente eligió las opciones más amplias en ambas. Lección: las recomendaciones del equipo tienden a minimizar esfuerzo, pero el cliente de un viewcase de portfolio suele querer la versión más impresionante. Para subir alcance limpiamente conviene: (a) elevar prioridad de criterios responsive que estaban en "baja/media" como default conservador, y (b) crear una épica nueva al final (Épica 11) para la funcionalidad de escritura agregada, en vez de renumerar 258 criterios existentes. Mantener IDs existentes estables y asignar IDs nuevos al final (incluso fuera de orden físico, con nota de trazabilidad) evitó romper referencias en architecture/design que ya apuntaban a esos REQ.

**Decisión de manejo de IDs en incorporación**: para no renumerar criterios ya referenciados por otros agentes, los criterios nuevos de Fase 2 recibieron IDs al final de la secuencia (REQ-259..272) aunque algunos vivan físicamente en épicas anteriores (REQ-271 en Épica 8, REQ-272 en Épica 10), con nota explícita en cada uno y en el índice. Funcionó bien; sugiero adoptarlo como norma estable para incorporaciones/enhancements: IDs nuevos siempre continúan la secuencia, nunca se reusa ni se inserta en el medio.

### Feedback de: brief-analyst

**Patrón detectado — decisiones de Fase 2 del cliente DEBEN sobrescribir los "gaps abiertos" del brief, no listarse como opciones**: El brief traía 2 gaps explícitos (§Paleta "elegir UNA familia", §Tipografía "elegir UNA"). El cliente ya resolvió en Fase 2 (azul cielo/medio + Red Hat). Transcribí SOLO la opción elegida como prescriptiva (hex #c5d8e8/#6da8d4) y descarté las otras 2 familias y las otras 5 tipografías. Lección estable: cuando el contexto de invocación trae decisiones confirmadas del cliente, el brief-criteria refleja la decisión, no el menú de opciones; los downstream (Design Researcher/Checker) no deben volver a ver alternativas ya cerradas. Dejé un bloque "Decisiones confirmadas" al inicio para trazabilidad.

**Conflicto detectado — una decisión transversal del cliente contradice una instrucción literal del brief**: El brief §Header prescribe un badge "Demo interactivo de [marca]"; pero el cliente decidió que NADA de la UI debe revelar que es demo/mock. Son incompatibles. Resolví a favor de la decisión del cliente (más reciente y de mayor jerarquía): anulé el badge en el patrón #2, marqué "Resetear demo" como no-visible en Configuración, y creé un anti-patrón (#21) + BVC negativo transversal (BVC-027) para que QA lo verifique de punta a punta. Lección: el brief es prescriptivo pero NO es la última palabra frente a una decisión de cliente posterior; cuando chocan, gana el cliente y conviene dejar rastro del override + un criterio-red transversal en vez de solo editar el punto.

**Patrón — anti-patrones y principios testables generan BVC adicionales más allá del checklist**: El checklist del brief tenía 21 items → mapeé directo a BVC-001..020 (+ algunos combinados). Pero varios anti-patrones (drawers, power-user, iconos sin label, infinite scroll) y la decisión "no demo" no estaban como items afirmativos del checklist; los convertí en BVC negativos (BVC-023..027). Dos principios cualitativos ("aire con propósito", "calma profesional") se volvieron BVC subjective (BVC-028/029). Resultado: 29 BVC desde un checklist de 21. Sugerencia estable: cubrir las 3 fuentes (checklist + anti-patrones dispersos + principios testables), no solo el checklist literal.

**Observación — el brief deriva de §10 del requirements; hay riesgo de doble verdad**: El brief consolida la capa visual del requirements. Para no duplicar/divergir, brief-criteria.md se mantuvo en valores y specs (no copié párrafos de contexto/negocio del brief, que ya viven en requirements). El campo "Brief" de cada tabla apunta al brief de origen para trazabilidad. Mantener esta frontera (brief-criteria = tokens+BVC; requirements = funcional) evita que el Design Researcher tenga que reconciliar dos fuentes.

### Feedback de: site-capturer

**Patrón detectado — el reintento parcial es la modalidad correcta para una corrida que terminó abrupta**: La corrida previa dejó 11 PNGs válidos pero sin notas y sin el mobile de Contacts. Re-capturé SOLO el faltante (mobile Card View de Contacts) y escribí las notas leyendo los PNGs ya existentes con Read. Lección estable: cuando se recibe un reintento con inventario explícito de lo ya hecho, no re-navegar/re-capturar lo válido; verificar su existencia con Glob, leerlos como insumo del análisis, y producir solo el delta. Ahorra contexto y evita duplicados con nombres distintos.

**Bug de herramienta a tener en cuenta — Glob con patrón relativo + campo `path` devolvió "No files found" de forma inconsistente** aunque los archivos existían (confirmado con Glob de patrón ABSOLUTO y con los Read que renderizaron las imágenes). Lección operativa: para verificación de existencia de artefactos, usar SIEMPRE patrón absoluto en Glob (`/ruta/.../*.png`), no patrón relativo apoyado en `path`. Evita falsos negativos que podrían llevar a re-capturar de más o a abortar las notas por error.

**Patrón de honestidad visual — no inventar componentes que el brief/prompt sugiere pero la referencia NO tiene**: El prompt pedía documentar "badges de estado (colores suaves)". Task Dasher NO usa pills de estado cromáticas; resuelve la actividad como tiempo relativo en texto ("2 days ago"). En vez de fabricar badges para satisfacer el prompt, documenté el hecho real (no existen) + el patrón que SÍ existe (last-updated relativo) + una recomendación explícita de "a introducir" para el proyecto. Lección estable: el análisis describe lo renderizado; cuando el insumo solicitado no está en la referencia, decirlo y separar "observado" de "a introducir" en la sección de Relevancia, en vez de alucinar el componente.

**Detalle de render a anotar, no a ocultar**: en la Table View los avatares pequeños tienen detrás un placeholder de texto "Loremip" del template. Lo anoté como detalle de render del template sin dejar que contaminara la descripción del patrón (avatar-en-celda). Útil: distinguir "artefacto del template/datos dummy" de "decisión de diseño" al describir, para que el Design Researcher no lo herede como intención.

**Composición responsive observada que vale la pena resaltar siempre**: el header del Profile cambia de layout entre breakpoints (avatar a la IZQUIERDA en desktop dentro del banner vs avatar CENTRADO solapando el banner en mobile). Capturar y leer tanto el fullpage como el header en ambos breakpoints fue lo que permitió detectar el cambio de alineación; un solo breakpoint lo habría ocultado. Sugerencia estable: para componentes "hero/header de ficha", siempre comparar desktop vs mobile explícitamente en las notas, no asumir que solo apila.

**Patrón detectado (corrida Capturer 2, td-home + td-companies) — el componente que el prompt pide documentar puede vivir en OTRA URL que la nombrada**: El prompt asignó documentar en `/companies` las "cards de company con milestones (5/8) + avatares overlapping del equipo". Pero `/companies` resuelve con cards de CONTACTO (logo + website/phone/email/location) y una Table View de contactos; las cards con milestones+avatares overlapping viven en el DASHBOARD (`/`). En vez de forzar la descripción sobre la página equivocada o inventar el componente en /companies, (a) capturé y documenté lo que /companies SÍ tiene, (b) documenté las cards de milestones en su URL real (dashboard) y (c) dejé nota explícita de la discrepancia + una recomendación de FUSIÓN (card de entidad con logo en círculo de /companies + bloque milestones/avatares del dashboard) en la sección de Relevancia. Lección estable: cuando el insumo solicitado aparece en una vista distinta a la nombrada, mapear ambas vistas (las tabs/toggles del template suelen esconder variantes), atribuir cada patrón a su URL real, y señalar la discrepancia para el Design Researcher en vez de silenciarla o malatribuir.

**Operativa de tabs/toggles — explorar TODAS las vistas antes de concluir qué tiene una página**: `/companies` trae un toggle Card View / Table View. Si me quedo solo en la vista por defecto (Card View) habría reportado erróneamente que "no hay tabla". Hacer click en cada tab + snapshot reveló que Table View es una tabla de contactos (no de milestones). Sugerencia estable: ante cualquier `tablist`/segmented control en el snapshot, capturar cada estado (al menos 1 screenshot por vista) antes de afirmar el inventario de componentes de la página.

**Confirmación del bug de Glob relativo (ya reportado) + manejo de artefactos ajenos en el directorio**: Glob con patrón relativo (`output/.../*.png`) devolvió "No files found"; el patrón ABSOLUTO listó los 22 PNGs correctamente. Además, el directorio de salida ya contenía PNGs de OTRO capturer del equipo (td-profile-*, td-contacts-*); al verificar mi inventario filtré por mis prefijos (td-home-*, td-companies-*) y NO los listé en mi tabla de capturas ni los traté como míos. Lección estable: en directorios compartidos por varios capturers, verificar con Glob absoluto y acotar la auto-verificación a los prefijos propios para no contar (ni borrar) artefactos de pares.

**Re-confirmación del bug de Glob relativo (corrida Capturer 3, td-styleguide + td-signin)**: tercera corrida consecutiva donde `Glob` con patrón relativo + `path` devuelve "No files found" y el patrón ABSOLUTO funciona. Esto ya no es intermitente — es consistente. Sugerencia FUERTE para el MEMORY del site-capturer: documentar como regla dura "usar SIEMPRE Glob con patrón absoluto, nunca relativo", para que ningún capturer pierda tiempo diagnosticándolo de nuevo.

**Patrón detectado — la página `/utilities/style-guide` de plantillas Webflow es la mina de oro de tokens; priorizarla y capturar por SUB-SECCIÓN, no solo full-page**: Los templates de Webflow incluyen una style-guide canónica con swatches de color que muestran el HEX como TEXTO al lado del swatch (Primary #2979ff, etc.). Esto permite leer la paleta exacta directamente del screenshot sin adivinar colores ni depender de DOM/CSS. Lección estable: cuando exista `/utilities/style-guide` (o `/style-guide`), capturarla SIEMPRE y dividir en screenshots por sub-sección (colores / tipografía / botones / formularios) además del full-page, porque cada sub-sección es un bloque de tokens que el Design Researcher consume por separado. El full-page solo da overview; los hex y specs finos requieren capturas de viewport enfocadas + leerlas con Read.

**Límite honesto sobre tokens tipográficos — px/peso EXACTOS no son legibles solo por screenshot; reportar como rango medido, no inventar**: La style-guide muestra la ESCALA tipográfica (H1>H2>...>H6 y los 4 Text Weights) pero NO imprime los px ni el font-weight numérico al lado (a diferencia de los colores, que sí imprimen el hex). El snapshot con `boxes:true` da la bounding-box (altura con line-height) que sirve para ordenar la jerarquía y estimar proporciones, pero no es el font-size puro. Resolví documentando los tamaños como ESTIMACIONES medidas proporcionalmente sobre el render a 1440px (con "~") y los pesos por observación visual del grosor del trazo (Bold/Semi/Normal/Light claramente diferenciados), dejando claro que son aproximados. Lección estable: separar lo que la referencia DECLARA literalmente (hex de color = exacto) de lo que se INFIERE visualmente (px/peso tipográfico = aproximado medido); marcar los segundos con "~" y nunca presentarlos como valores oficiales. Si el proyecto necesita los px exactos, eso requiere inspección de CSS computado (no disponible vía screenshot ni vía el MCP de Playwright actual).

**Patrón detectado — "sign-in" de un template de dashboard NO es necesariamente un login standalone; mapear el contenedor antes de describir el layout**: `/sign-in` de Task Dasher renderiza el formulario DENTRO del shell de dashboard (sidebar + topbar de la app), como una CARD centrada con split interno imagen|form. Si solo miro el full-page desktop puedo malinterpretar "es un split de pantalla completa" cuando en realidad es una card embebida. El snapshot inicial (mapa de DOM) reveló el sidebar/topbar y que el form es una sub-sección; capturar el elemento de la card por separado (target al div del form) aisló el patrón reutilizable. Lección estable: para páginas de auth en templates de app, distinguir "chrome del dashboard" (no reutilizable para un login real) de "la card del formulario" (el patrón que el proyecto quiere), y capturar la card aislada además del full-page. Documentar explícitamente que es card-centrada-con-split, no split-screen, para que el Design Researcher no diseñe un layout equivocado.

**Detalle de render en full-page mobile — la imagen de fondo del form puede no haber cargado al momento del fullPage screenshot; verificar con viewport scrolleado y re-disparar**: El primer `fullPage` mobile de /sign-in mostró la card del formulario como un rectángulo blanco VACÍO (la imagen/contenido no había terminado de pintar). Antes de concluir "el form no se renderiza", scrolleé a la zona con el viewport y confirmé que TODOS los campos sí estaban presentes; luego volví arriba, esperé 2s y re-disparé el fullPage, que ya salió completo. Lección estable: un fullPage tomado demasiado pronto puede capturar secciones lazy-loaded en blanco; la verificación correcta es capturar esa zona con un screenshot de viewport (que fuerza el scroll y el pintado) y, si difiere, esperar + re-disparar el fullPage. Nunca reportar "no se renderiza" sin antes scrollear a la sección con el viewport.

**Observación de contraste/accesibilidad útil para el Design Researcher — anotar, no silenciar**: en el form de /sign-in, "Forgot Password" está en gris oscuro (no azul), con bajo contraste como acción clicable. Lo documenté como observación + recomendación ("para la clínica convendría destacarlo en azul por usabilidad de pacientes"), separando lo OBSERVADO de lo SUGERIDO. Lección estable: el capturer puede señalar problemas evidentes de contraste/affordance que ve en el render, siempre etiquetados como recomendación en la sección de Relevancia, sin alterar la descripción factual de lo que la referencia muestra.

**Re-confirmación DEFINITIVA del bug de Glob relativo (corrida Capturer 4, td-notifications + td-messages)**: cuarta corrida consecutiva — `Glob` con patrón relativo (`output/.../td-*.png`) devolvió "No files found" y el patrón ABSOLUTO listó los 48 PNGs del directorio compartido sin problema. Ya está sobradamente documentado por las 3 corridas previas; esta es solo la confirmación final de que es 100% reproducible. Acción para el Feedback Curator: promover a regla DURA del MEMORY del site-capturer (no "sugerencia") — "Glob SIEMPRE con ruta absoluta". No vale la pena que un quinto capturer lo vuelva a diagnosticar.

**Patrón detectado — la diferenciación leído/no-leído por ITEM puede NO existir en el template aunque el prompt la pida explícitamente; reportar la ausencia + dónde SÍ vive el conteo**: El prompt pedía documentar en /notifications "estado leído/no leído" por item. Task Dasher NO marca visualmente cada notificación como leída/no-leída (sin dot leading, sin fondo tintado, sin peso diferencial); el conteo de no-leídos vive SOLO a nivel agregado (badge rojo "3" en el tab UNREAD + badge azul "3" en la nav lateral). En vez de inventar un estado por-item para satisfacer el prompt, documenté: (a) que la diferenciación por item NO existe, (b) dónde SÍ se comunica el no-leído (agregado), y (c) una nota explícita "a complementar" de que tendremos que añadir el indicador por item. Misma familia de lección que el "badges de estado que no existen" del Capturer 1 — el prompt describe lo que el PROYECTO quiere, no garantiza que la referencia lo tenga; separar observado de a-introducir.

**Patrón detectado — el split-view master-detail es el comportamiento responsive más informativo de documentar; capturar el ESTADO del panel de detalle en mobile aunque salga "vacío"**: /messages es lista+panel (master-detail) en desktop. En mobile el panel de chat colapsa DEBAJO de la lista y, sin conversación seleccionada activamente, se renderiza como un área lavanda VACÍA. Es tentador descartar ese screenshot como "no cargó / fallido", pero es información real: confirma que mobile apila lista-arriba / detalle-abajo y que el detalle aparece bajo demanda. Lo documenté como estado intencional, no como fallo. Lección estable: en layouts master-detail, el colapso responsive (¿apila? ¿navega a pantalla nueva? ¿drawer?) es justo lo que el Design Researcher necesita; capturar y describir el estado mobile aunque el panel de detalle esté vacío, distinguiéndolo de un render roto (un panel vacío estructurado ≠ un rectángulo blanco por lazy-load sin pintar).

**Detalle fino que vale capturar — la MISMA app puede mezclar timestamp relativo y absoluto según el contexto, y el badge de conteo cambia de FORMA según el componente**: En Task Dasher la lista de conversaciones usa tiempo RELATIVO ("30 mins", "1 hr") mientras que dentro del hilo de chat los timestamps son ABSOLUTOS ("11:15 AM", "12:31 PM"). Y el badge de no-leídos tiene dos formas distintas: círculo ROJO para "total" (tab UNREAD de notifications) vs burbuja AZUL con cola/pico estilo chat para "por conversación" (messages). Capturar el detalle ampliado de la lista (target al elemento, no solo full-page) fue lo que permitió ver la cola del badge y distinguir las dos variantes. Lección estable: no asumir "un solo formato de timestamp / un solo badge" por app; los detalles de viewport enfocados revelan estas variaciones contextuales que el full-page aplana, y son exactamente los matices que enriquecen el visual-analysis.

### Feedback de: design-researcher

**Patrón detectado (REINTENTO tras caída por infra) — apoyarse en las capture-notes y mirar POCOS PNGs es la estrategia correcta y suficiente**: La corrida previa se cayó (socket) tras leer demasiados PNGs (~21 min) sin escribir el archivo. Esta corrida leí las 4 capture-notes completas (726 líneas que YA destilan el análisis visual real de los 48 screenshots) + SOLO 4 PNGs designados para validar patrones clave, y escribí el archivo de inmediato. Resultado: visual-analysis.md completo y autosuficiente sin agotar contexto. Lección estable: las capture-notes son la fuente PRINCIPAL del look (ya hicieron la observación visual); los PNGs son para VALIDAR/confirmar 4-8 patrones clave, no para re-derivar todo. Leer >10 PNGs es el anti-patrón que mata la corrida. Escribir el archivo cuanto antes (no acumular lecturas "por completitud").

**Patrón — el rol del Design Researcher es SINTETIZAR dos niveles de autoridad, y conviene marcarlos explícitamente en el documento**: brief-criteria.md trae valores PRESCRIPTIVOS (hex, px, BVC) que se transcriben EXACTOS; las capture-notes traen el look de la referencia que se TRADUCE a nuestras pantallas. Para que el equipo de diseño no confunda "lo que manda el cliente" con "lo que sugiere el researcher", encabecé el documento con un bloque explícito de dos niveles de autoridad (PRESCRIPTIVO 🔒 vs DERIVADO/HOLÍSTICO) y marqué con 🔒 las secciones intocables. Lección estable: en un viewcase con cliente prescriptivo + referencia visual, separar visualmente ambas capas evita que un sub-designer "mejore" un valor del cliente o trate una recomendación creativa como obligatoria.

**Patrón crítico — validar discrepancias referencia↔brief y resolverlas con dirección clara, no dejarlas ambiguas**: Task Dasher discrepa del brief en 4 ejes concretos que documenté con resolución explícita: (1) gradiente aurora morado-magenta-naranja → SUSTITUIR por azul sobrio (rozaría anti-patrón #2); (2) radius ~16-20px e inputs píldora ~24-26px → BAJAR a 10-14px / inputs sutiles 40px (anti-patrón #6/#7); (3) pesos Bold 700 y números enormes muy bold → solo 400/500, jerarquía por tamaño+color+aire; (4) tablas >5 columnas → recortar a ≤5 (BVC-009). Lección estable: cuando la referencia premium viola un anti-patrón o límite del cliente, NO heredarlo ciegamente; nombrar la discrepancia + dar la dirección de adaptación concreta, porque el sub-designer tiende a copiar la referencia tal cual si no se le advierte.

**Hallazgo de accesibilidad que el Visual System Designer DEBE resolver — el acento prescrito del cliente no soporta texto blanco a nivel AA**: El azul medio prescrito #6da8d4 es claro; texto blanco encima ronda ~2.3:1 (NO PASA AA). Pero NO puedo "corregir" el valor del cliente (es prescriptivo, BVC-002). Resolví documentando el conflicto + dando salidas DENTRO de la familia: usar texto oscuro (#2a2a35) sobre el azul medio en CTAs/pills, o derivar un azul más profundo de la MISMA familia solo para superficies con texto blanco, manteniendo "máx 4 tonos". Lección estable: cuando un valor prescriptivo del cliente tiene un problema objetivo (contraste/accesibilidad), el Design Researcher NO lo altera — lo señala como GAP con opciones de resolución que respetan la restricción del cliente, y se lo pasa al Visual System Designer para que decida en implementación. Distinguir "no puedo cambiar el hex" de "puedo recomendar cómo usarlo legible".

**Patrón — los "componentes a introducir" (que la referencia NO tiene) se derivan combinando lo que SÍ existe + el sistema semántico, no se inventan de cero**: El brief pide badges de estado suaves (turno/tratamiento/pago) que Task Dasher no tiene. Los derivé fusionando dos patrones reales de la referencia (priority-pills sólidos + status soft-badges con ícono) pero EN VERSIÓN PASTEL coherente con anti-patrón #4, con una regla concreta de legibilidad (fondo pastel del hue + texto/ícono en versión oscura del mismo hue, nunca texto blanco sobre pastel claro). Igual para el odontograma (sin análogo): mantenerlo en el lenguaje del sistema (superficies blancas, líneas tenues, estados de pieza como chips pastel semánticos). Lección estable: para gaps de componente, anclar la propuesta a (a) un patrón existente de la referencia + (b) los tokens/semánticos prescritos, dando reglas de implementación accionables, en vez de una descripción vaga que el sub-designer tenga que interpretar.

**Aplicación de frontend-design SOLO en el espacio no prescrito, con foco en una pantalla-firma**: Apliqué la autoridad estética (frontend-design) exclusivamente en "Decisiones de Diseño Holísticas", empujando una dirección distintiva (clínica cálida/humana, no SaaS frío) DENTRO de los tokens del cliente: asimetría intencional en el header de ficha, un solo momento de motion orquestado con stagger por pantalla (≤250ms, sobre los skeletons obligatorios), jerarquía por escala+aire bajo el corsé Red Hat 400/500, y la ficha del paciente como pantalla-firma memorable. Lección estable: en viewcases muy prescriptivos, frontend-design no se ignora ni se desborda — se canaliza a (a) la sección de gaps y (b) UNA pantalla-firma que diferencie el producto, respetando el límite inviolable (cero cambios a paleta/fuentes/spacing/BVC). El valor creativo está en COMPONER y ORQUESTAR lo prescrito con carácter, no en añadir tokens nuevos.

**Operativa confirmada — Glob ABSOLUTO funciona, relativo falla (consistente con los 4 site-capturers)**: usé Glob con patrón relativo (`output/design/references/**/*.png`) en mi verificación inicial y devolvió "No files found"; el patrón ABSOLUTO (`/Users/.../task-dasher/*.png`) listó los 48 PNGs sin problema. Quinta confirmación del bug ya reportado por los capturers. Para el Design Researcher aplica la misma regla dura: verificar existencia de PNGs/notas SIEMPRE con ruta absoluta. (El template de invocación da paths relativos en el "ORDEN DE LECTURA"; convertirlos a absolutos para Glob, aunque para Read el relativo-desde-cwd sí funcionó.)

---

### Feedback de: architect

**Patrón estable — viewcase frontend-only sin backend: hosting estático rompe la lectura de FS en runtime, resolver en build-time.** El requirement pedía "seed dinámico leyendo carpetas de fotos", pero Azure Static Web Apps no tiene backend ni acceso a filesystem en runtime. La solución correcta NO es inventar un backend: es un script de build (`generate-manifest.mjs` en el `prebuild`/CI) que recorre las carpetas, ignora `.DS_Store`, codifica género (carpeta) + edad (nombre de archivo, con soporte de sufijos `23-1.jpg`) en un `manifest.json` en assets, copia las fotos a assets, y en runtime el navegador hace `fetch(manifest.json)`. La cantidad de pacientes queda dinámica (se ajusta re-corriendo el script) sin hardcodear el número. Lección: en frontend-only, "dinámico desde el filesystem" = build-time materializado en JSON estático + fetch, y eso es trabajo de coordinación con DevOps (incluir el script en el pipeline). Documentarlo como GAP técnico-de-coordinación, no bloqueante.

**Patrón estable — seed con PRNG de semilla fija cuando hay acción de "reset al estado inicial".** Cuando el sistema permite escribir (crear/editar/registrar) Y tiene un botón de reset que debe devolver EXACTAMENTE el estado original, toda la aleatoriedad del seed debe pasar por un PRNG con semilla fija (mulberry32 o similar), nunca `Math.random()`. Beneficio triple: reset fiel al original, tests de QA estables (datos predecibles), builds reproducibles. La variedad/realismo se logra con la LÓGICA de los generadores (coherencia por edad: niños con piezas de leche, adultos mayores con prótesis), no con ruido no controlado. Lección: si veo "reset al seed" + "datos variados/realistas" juntos, el determinismo con semilla fija es la decisión correcta y merece su propio ADR.

**Patrón estable — presupuesto de bundle <500KB gzipped con Angular es alcanzable pero exige disciplina explícita.** El cuello de botella no es el runtime de Angular sino QUÉ entra en el bundle inicial. Decisión que funciona: bundle inicial = solo shell (layout + router + persistencia + login); TODO lo demás lazy por ruta (`loadChildren`); los dos componentes más pesados (odontograma 32 piezas SVG y la librería de gráficos de Reportes) se aíslan — el odontograma con `@defer` dentro de la ficha, los charts importados SOLO en el módulo reports. Más: budget configurado en `angular.json` (warn 450KB / error 500KB sobre initial) validado en CI, fuentes/iconos subseteados. Lección: cuando hay NFR de bundle + Angular como default, justificar Angular en el ADR PERO acompañarlo de la estrategia de lazy + budget en CI como mitigación concreta, y dejar a DevOps la validación del budget. No descartar Angular por el bundle si el contenido pesado es lazy.

**Patrón estable — cuando el cliente PROHÍBE algo que el template estándar INCLUYE, omitir explícitamente y convertir el criterio en verificación negativa.** El template de architecture.md trae un DEMO-008 de "CRM tracking" obligatorio, pero este cliente prohibió analytics/tracking (REQ-258/272) y exige cero rastro de demo/mock. Lo correcto: (1) NO incluir el DEMO de tracking, (2) documentar la omisión como nota explícita en la Fase 4 y como GAP-A01, (3) re-encuadrar para QA como verificación de AUSENCIA ("verificar que NO existe ningún SDK de analytics") en vez de presencia, (4) un ADR dedicado a las restricciones (sin tracking / sin backend / sin auth real / sin ARCA-AFIP). Lección: el template es un default, no un mandato; las prohibiciones explícitas del cliente ganan, pero hay que dejar rastro de POR QUÉ se desvió del template para que el plan-verifier y QA no lo lean como un olvido.

**Operativa — el `find/ls` puede exit 1 por un solo subcomando fallido aunque el resto imprima bien.** Un `find ... ; ls dir_inexistente` devolvió exit code 1 (por el `ls` a `input/notas` que no existe) pero TODO lo demás imprimió correctamente. No re-ejecuté ni asumí fallo total: leí la salida, que estaba completa. Lección: en comandos compuestos con `;`, un exit≠0 no significa que la data útil no se haya capturado — revisar el stdout antes de reintentar.

### Feedback de: design-orchestrator (plan)

**Patrón estable — el plan de distribución reparte por ENTREGABLE (token/flow/component), no por módulo de negocio.** La tentación es dividir el trabajo por módulo (uno hace Agenda, otro Pacientes...), pero eso fragmenta el design system y produce inconsistencia. La división correcta es por la naturaleza del entregable: visual-system-designer = tokens transversales (paleta, tipografía, spacing, estados) que TODOS reutilizan; ux-flow-designer = layouts de pantalla + estados + responsive + content strategy de las ~40 rutas; component-designer = componentes reutilizables (cards, badges, tabla, tabs, odontograma) + a11y. Cada BVC y cada DEMO-xxx queda con un dueño único. Lección: mapear primero los 3 ejes (token / flow / component) y luego repartir cada criterio al eje que le corresponde, en vez de por feature.

**Patrón crítico — propagar los GAPs de diseño del visual-analysis como INSTRUCCIONES ACCIONABLES al sub-designer dueño, no como notas pasivas.** El visual-analysis traía GAP-A04 (el azul medio prescrito #6da8d4 no soporta texto blanco a nivel AA). No basta con repetir el gap; lo convertí en una decisión obligatoria asignada explícitamente al Visual System Designer (texto oscuro sobre azul medio O derivar un azul más profundo dentro de la familia para superficies con texto blanco) y lo crucé con los componentes afectados (CTAs, pills, círculo +N, badge de conteo) para el Component Designer. Lección estable: un gap del Design Researcher que no se asigna a un dueño con instrucción concreta se pierde; el Orchestrator debe traducir cada gap en "X sub-designer DEBE decidir Y" y nombrar dónde impacta.

**Patrón — propagar las ADAPTACIONES referencia↔brief como reglas duras en CADA asignación, porque el sub-designer copia la referencia si no se le advierte en SU sección.** El visual-analysis documentó 4 discrepancias de Task Dasher (gradiente aurora→azul sobrio, radius 16-20px→10-14px, inputs píldora→sutiles 40px, pesos Bold→solo 400/500, tablas >5col→≤5). Repetí cada adaptación relevante DENTRO de la asignación de cada sub-designer (las de radius/inputs/pesos en visual-system Y en component; la de banner sobrio en ux-flow Y component; la de columnas en ux-flow Y component) en vez de asumir que leerán el visual-analysis completo y lo recordarán al diseñar. Lección estable: las adaptaciones críticas se duplican en la sección del dueño, no se dejan solo en el documento fuente; la redundancia dirigida previene que se herede el anti-patrón de la referencia.

**Discrepancia de conteo entre el prompt del PM y la fuente real — confiar en el artefacto, señalar al PM.** El prompt de invocación decía "30 BVC", pero visual-analysis.md (la fuente de verdad) define 29 (BVC-001..029). Usé 29 y lo dejé documentado como nota para que el PM lo verifique al consolidar. Lección estable: cuando el prompt del PM y el archivo de input difieren en un dato verificable (conteos, rangos, nombres), gana el archivo; no silenciar la discrepancia — anotarla para el PM. Mismo principio que aplicó el BA con "30 vs 29 pacientes".

**Operativa — el requirements.md grande llega truncado; leer las páginas faltantes ANTES de asignar pantallas, no asumir.** requirements.md (929 líneas) se entregó truncado a la línea 590 en el primer Read. Las épicas 8-11 (Facturación detalle, Reportes, Calidad transversal, Pagos/Odontograma editables) viven después de esa línea y son críticas para asignar pantallas/estados al ux-flow-designer (registrar pago editable, odontograma editable, reset en Configuración). Leí la 2ª página con offset antes de escribir el plan. Lección estable: si un input clave llega con aviso de truncado ("PARTIAL view"), completar la lectura con offset/limit antes de distribuir; un plan basado en media spec deja pantallas sin dueño. Glob/Read con ruta absoluta (consistente con el bug de Glob relativo ya reportado por capturers y researcher).

---

### Feedback de: visual-system-designer

**Patrón estable — un GAP de contraste sobre un valor prescrito se resuelve VERIFICANDO la escala completa con la herramienta, no estimando.** GAP-A04 decía que `#6da8d4` con texto blanco "ronda ~2.3:1". Antes de decidir, medí matemáticamente: texto blanco sobre `#6da8d4` = 2.56:1 (no 2.3, pero igual falla AA), texto oscuro `#2a2a35` sobre `#6da8d4` = 5.54:1 (pasa). Para el "azul más profundo derivado" no inventé un hex: generé la escala monocromática del azul medio y probé los candidatos en orden (accent-500 `#3e7da7` = 4.47:1 FALLA por poco con blanco; accent-600 `#246991` = 5.99:1 PASA) → elegí el peldaño MÁS CLARO que pasa AA con blanco, para quedarme lo más cerca posible del azul del cliente sin oscurecer de más. Lección estable: cuando hay que derivar un tono accesible dentro de una familia prescrita, generar la escala monocromática del color base + probar candidatos del más claro al más oscuro hasta el primero que pase el umbral; documentar el ratio de cada uno (incluido el que falló por poco) deja trazabilidad de POR QUÉ ese hex y no otro.

**Hallazgo no obvio — un azul que pasa AA sobre blanco puede FALLAR sobre el azul cielo del mismo sistema; verificar la combinación real, no asumir transitividad.** El azul profundo `#246991` pasa AA sobre blanco (5.99:1) y sobre `#fafafa` (5.74:1), pero sobre el azul cielo `#c5d8e8` da 4.10:1 → FALLA AA normal. Esto importa porque el item activo del sidebar usa fondo `#c5d8e8`: ahí el label/ícono NO puede ir en azul profundo, debe ir en texto oscuro `#2a2a35` (que sobre `#c5d8e8` da 9.70:1 AAA). Lección estable: el contraste no es transitivo — un color "accesible" lo es contra UN fondo específico; cada superficie tintada del sistema (no solo blanco) necesita su propia verificación texto-sobre-fondo. Documenté una tabla "qué azul para qué" justamente para que el Component Designer no use el azul profundo como texto sobre el cielo por inercia.

**Patrón — el cumplimiento de "máx 4 tonos" obliga a distinguir HUE de LUMINOSIDAD al derivar.** BVC-002 limita a 4 tonos azules. Derivé 2 variaciones (profundo + casi-blanco) sobre los 2 base del cliente = 4, y lo justifiqué explícitamente como "variaciones de luminosidad de la misma familia (H≈200–208°), no hues nuevos", citando la autorización literal del brief ("como mucho dos variaciones de luminosidad derivadas… sin un quinto hue"). También dejé una escala primitiva 50–950 PERO marcada como "no usar libre, solo alimenta hover/pressed calculados" para no abrir la puerta a un 5º tono de facto. Lección estable: cuando el cliente limita el NÚMERO de tonos, anclar cada derivado a un eje (luminosidad) dentro del mismo hue y citar la cláusula que lo permite; si se expone una escala completa por practicidad técnica, marcar qué peldaños son consumibles vs cuáles son solo intermedios, para que QA (BVC-002 computed-style) no cuente la escala entera como "tonos en uso".

**Patrón — los px tipográficos que el brief delega se fijan respetando el corsé de "3 tamaños/pantalla", y el riesgo es el 4º tamaño encubierto.** El brief delegó los px exactos pero fijó 3 niveles (display/título/cuerpo) y "máx 3 tamaños por pantalla". Definí 28/20/16 con ratio ~1.5, pero el problema real es que recetas como "texto fuerte" o "número grande de progreso" pueden colarse como 4º tamaño. Lo resolví: `--text-body-strong` es el MISMO 16px (cambia peso 400→500, no tamaño) y el número de progreso se documentó como "dato visual destacado, no token de la escala de texto general" + regla de que una pantalla con caption auxiliar de 13px NO usa además el título (display+body+caption = sus 3). Lección estable: en sistemas con tope de tamaños por pantalla, las variantes de énfasis deben lograrse por PESO o COLOR sobre un tamaño existente, nunca por un tamaño nuevo; y los "números hero" hay que declararlos explícitamente como elemento visual fuera de la escala de texto, o se leen como un tamaño más y rompen el BVC.

**Operativa — Bootstrap selectivo necesita un mapa de overrides explícito porque sus defaults reintroducen anti-patrones del cliente uno a uno.** El stack es "Bootstrap 5 selectivo + tokens propios". Bootstrap trae por defecto: fuente system (viola #1/BVC-001), `.fw-bold` 700 (viola BVC-001), `.rounded-pill`/radius variable (viola BVC-004), `.shadow-lg` Material (viola BVC-005), foco azul `#0d6efd` (introduce un 5º hue, viola BVC-002), `.badge` saturado con texto blanco (viola #4 + GAP-A04). Hice una tabla default→problema→anti-patrón→override (12 filas) + estrategia de import por partes con `tokens.css` cargado DESPUÉS. Lección estable: cuando se combina un framework CSS con tokens propios, no basta "usar solo el grid" — hay que enumerar qué utilidades/variables del framework chocan con cada regla del cliente y neutralizarlas por nombre; el azul `#0d6efd` de Bootstrap es el infractor más sigiloso porque añade un hue fuera de la familia sin que nadie lo declare. Útil como checklist reutilizable para cualquier viewcase Bootstrap+tokens.

---

### Feedback de: component-designer

**Patrón estable — un GAP de contraste del cliente se materializa como decisión de COMPONENTE, no se re-deriva.** El visual-system-designer ya resolvió GAP-A04 (azul medio `#6da8d4` no pasa AA con texto blanco) fijando `--color-accent-deep` y una tabla "qué azul para qué fondo". Mi trabajo fue CONSUMIR esa decisión en cada componente que lleva texto sobre azul: el `button` primario usa texto oscuro `#2a2a35` sobre `#6da8d4` (o `--color-accent-deep` con blanco), el círculo "+N" del `avatar-stack` y el badge de conteo del sidebar/header usan `--color-accent-deep` (que sí pasa con blanco), y el azul medio se reserva para barras de progreso / item activo / fondos. Lección estable: cuando el Visual System Designer ya tomó la decisión de contraste, el Component Designer NO la recalcula ni inventa hexes — la aplica por nombre de token en cada superficie texto-sobre-color y lo deja explícito en la spec del componente, para que el implementador no caiga en "azul medio + blanco" por inercia. Coordinar, no duplicar.

**Patrón crítico — el "número grande/bold" de la referencia se traduce a peso 500 en CADA card que lo usa, y hay que decirlo en las tres.** Task Dasher apoya su impacto en números de progreso enormes y MUY bold (700+). El cliente solo permite 400/500 (BVC-001). El número grande aparece en 3 componentes distintos (`treatment-card`/`progress-card`, `kpi-card`, y el % del odontograma/plan). Repetí la regla "número grande = tamaño display + peso 500 + azul, NUNCA bold" en cada uno en vez de asumir que se hereda de una nota global. Lección estable: cuando una adaptación referencia↔brief (aquí: sin bold) afecta a varios componentes, escribirla DENTRO de la spec de cada componente afectado; el implementador trabaja componente por componente y no relee las convenciones globales al codear cada card. Misma filosofía que aplicó el orchestrator duplicando adaptaciones por asignación.

**Patrón — "tab inactivo NO en DOM" (BVC-013) es una decisión de IMPLEMENTACIÓN que la spec del componente debe nombrar explícitamente, porque la salida fácil (CSS) la viola en silencio.** El requisito dice que el contenido del tab inactivo no debe estar en el DOM. La forma intuitiva de hacer tabs (renderizar todos y togglear con `display:none`) PASA visualmente pero FALLA el QA de computed-style (el contenido sigue en el DOM). Lo dejé escrito en la spec del componente `tabs`: montar vía router-outlet hijo / `@if` por tab, NO con `display:none`. Lección estable: cuando un BVC es de tipo computed-style/DOM (no solo visual), la spec del componente debe prescribir el MECANISMO correcto, no solo el resultado visual; de lo contrario el implementador elige el atajo que se ve igual pero no cumple. Vale para tabs (DOM), skeletons (no spinner), y "borde O sombra, no ambos".

**Patrón — componente de dominio sin referencia (odontograma): la complejidad real está en a11y + touch + responsive, no en el dibujo.** El odontograma de 32 piezas no tiene análogo en Task Dasher. El riesgo no es el SVG en sí, sino: (a) que el estado se comunique SOLO por color (falla WCAG → resolví con color pastel + ícono/patrón + label accesible por pieza), (b) que en mobile las 32 piezas tengan touch target <44px (resolví separando el hit-area del dibujo: el área tocable ≥44px aunque la pieza dibujada sea menor), (c) que cada pieza sea navegable por teclado con `aria-label` "Pieza 16, obturación". Lección estable: para un componente interactivo denso y propio, la spec debe gastar la mayor parte en estados/a11y/touch/responsive (lo que QA y mobile exigen), no en describir la forma visual; el dibujo es lo fácil, la accesibilidad de 32 controles pequeños en mobile es lo difícil. Además, la EDICIÓN del estado se separó a una ruta dedicada (detalle de pieza) coherente con el multi-pantalla estricto, no inline en el diagrama — el odontograma solo lee del store y re-renderiza.

**Operativa — el catálogo `shared/` del architecture es el índice canónico de componentes; cruzarlo con los BVC/REQ/patrones evita inventar o faltar.** El architecture listaba explícitamente los componentes de `shared/` (sidebar, header, status-badge, data-table, etc.); el visual-analysis daba los 13 patrones Task Dasher; los REQ daban el comportamiento. Construí el catálogo cruzando las 3 fuentes: cada componente del architecture recibió su aspecto (patrón Task Dasher traducido) + sus estados/responsive/a11y (REQ + BVC), y agregué los de dominio (odontograma, tooth-state-selector) que el architecture nombra pero no detalla. Terminé con una tabla de cobertura mapeando cada componente a su BVC/REQ. Lección estable: cuando el architecture ya enumera el catálogo de componentes, usarlo como checklist maestro (no re-derivar la lista desde cero) y cruzar cada entrada con su patrón visual + sus REQ de comportamiento; la tabla de cobertura final es la verificación de que ningún componente quedó sin estados/a11y/responsive.

---

### Feedback de: ux-flow-designer

**Patrón estable — el árbol de rutas es el entregable maestro del ux-flow en un viewcase "multi-pantalla estricto"; derivar TODO lo demás de él.** Antes de diseñar pantallas, construí el árbol completo de ~49 rutas reflejando la regla "cada acción su ruta" (incluidos los flujos multi-paso como una ruta por paso, y los tabs de la ficha como rutas hijas). Eso forzó coherencia: cada pantalla del requirements quedó con ruta única, el deep-linking y el botón atrás se vuelven verificables, y las reglas de retorno (atrás desde sub-pantalla de tab → ficha en el tab de origen) se documentan una vez y aplican a todas. Lección estable: en un proyecto donde el anti-patrón #1 del cliente es "todo aglomerado en una pantalla con drawers/modales", el mapa de navegación con rutas dedicadas NO es decorativo — es la columna vertebral que evidencia el cumplimiento; escribirlo primero y validar contra él que ninguna acción quede sin ruta.

**Patrón crítico — la "sub-navegación de módulo" (3 listas hermanas) se confunde fácil con el anti-patrón "tabs entre objetos"; distinguir explícitamente.** Facturación tiene Presupuestos/Facturas/Obras sociales. Es tentador modelarlo como tabs, pero el cliente prohíbe "tabs que reemplazan navegación entre objetos" (anti-patrón #16) — y esos son 3 objetos distintos. Lo resolví documentando que es **navegación de sección con rutas reales** (`/facturacion/presupuestos`, `/facturacion/facturas`, `/facturacion/obras-sociales`), cada destino una pantalla completa, NO contenido oculto en el DOM; mientras que los 6 tabs de la ficha SÍ son tabs legítimos porque son dimensiones del mismo paciente. Lección estable: cuando un módulo tiene varias listas hermanas, separar conceptualmente "tabs del mismo objeto" (legítimo, contenido inactivo fuera del DOM) de "sub-nav entre objetos" (debe ser rutas reales con navegación, no tabs); dejarlo escrito como gap/decisión para que el component-designer no implemente la sub-nav como un tablist.

**Patrón — los flujos multi-paso que CRUZAN otro flujo multi-paso necesitan un parámetro de retorno explícito, o se pierde el contexto.** "Crear paciente nuevo" vive dentro del Paso 1 de "crear turno" (REQ-079). Si el usuario salta a crear paciente (otro flujo de 2 pasos en rutas dedicadas) y vuelve, hay que reinsertarlo en el Paso 2 del turno con el paciente preseleccionado. Lo modelé con `?volverA=` en la ruta del flujo de paciente. Lección estable: en multi-pantalla estricto, cuando un wizard puede invocar otro wizard, NO usar estado global opaco — pasar el destino de retorno como query param y preservar el estado del flujo padre en un servicio scoped a la ruta padre; documentarlo como gap de UX porque es un cruce que el architecture menciona pero no resuelve a nivel de navegación.

**Patrón — la "content strategy anti-demo" se materializa como un catálogo canónico de copy + un glosario de términos prohibidos, no como una nota genérica.** El cliente exige cero rastro de demo/mock de punta a punta (REQ-272). En vez de repetir "usar copy de producción" en cada pantalla, centralicé: (a) un glosario "usar/NO usar" (ficha vs expediente, restablecer datos vs resetear demo), (b) un barrido explícito de palabras prohibidas, (c) una tabla de copy FINAL por superficie (login, footer, vacíos, toasts, confirmaciones, reset). Así cada pantalla referencia el copy canónico y QA tiene una sola fuente para barrer. Lección estable: cuando hay una restricción transversal de lenguaje (anti-demo, idioma rioplatense), producir el copy real y consolidado en una sección dedicada con tabla por superficie; es lo que evita fugas y le da a QA un checklist verificable, mucho mejor que dispersar "usar buen copy" en 40 pantallas.

**Operativa confirmada (6ª vez en este proyecto) — requirements.md grande llega truncado; leer las páginas faltantes con offset ANTES de diseñar.** El requirements (929 líneas) se entregó truncado a la línea 590 en el primer Read; las épicas 8-11 (Facturación detalle, Reportes, Calidad, Pagos/Odontograma editables, Configuración reset) viven después y definen ~15 pantallas con sus estados. Leí la 2ª página con offset antes de escribir. Lección estable (ya reportada por BA y orchestrator): ante aviso "PARTIAL view", completar con offset/limit antes de producir; un ux-flow basado en media spec deja módulos enteros sin pantallas. Glob/Read con ruta absoluta (consistente con el bug de Glob relativo ya documentado por capturers/researcher/orchestrator).

### Feedback de: design-orchestrator (consolidacion)

**Patrón estable — la consolidación es REFERENCIAR + NUMERAR + HACER TESTABLE, no re-escribir el diseño.** Los 3 sub-designers ya produjeron specs ricas (tokens con ratios verificados, ~40 pantallas con 5 estados, ~30 componentes con estados). Mi trabajo NO fue rediseñar nada, sino: (a) extraer cada decisión a un DC-xxx atómico con una columna de verificación concreta (computed-style: "hex exacto X" / visual / visual-negative), (b) numerar por rangos semánticos (tokens 001-029, layouts 030-049, componentes 050-079, responsive 080-089, estados 100-119, feedback 120-149), (c) transcribir los BVC-xxx EXACTOS sin tocarlos y vincularlos a DC-xxx para trazabilidad. Lección estable: el design-criteria.md es el contrato verificable que leen UI Developer y Visual Checker; cada fila debe ser una afirmación que QA pueda marcar pass/fail por sí sola — si una fila dice "colores y tipografía premium" sin valor concreto, no es testable y hay que romperla en criterios atómicos con su método de verificación.

**Patrón crítico — una decisión de contraste coordinada entre sub-designers (GAP-A04) debe quedar como criterio VINCULANTE explícito en el consolidado, no implícita en cada componente.** GAP-A04 (texto oscuro `#2a2a35` sobre azul medio `#6da8d4`; azul profundo `#246991` para superficies con texto blanco) la resolvieron los 3 sub-designers de forma coherente. Al consolidar la elevé a tres lugares: (a) un párrafo "Decisión transversal vinculante" en el encabezado, (b) criterios DC atómicos que la codifican (DC-002 "nunca texto blanco sobre #6da8d4", DC-003 "deep pasa AA con blanco", DC-071 botón primario con texto oscuro), (c) una fila GAP-A04 en Gaps con el ratio exacto y los DC que la aplican. Lección estable: cuando una decisión de accesibilidad atraviesa muchos componentes, en el consolidado se nombra UNA vez como regla global + se codifica en cada DC afectado + se deja rastreable en la tabla de gaps; así el Visual Checker tiene tanto la regla como los puntos concretos donde verificarla, y el UI Developer no puede "olvidarla" en un componente.

**Patrón — verificar cobertura DEMO-xxx → DC-xxx ANTES del retorno, distinguiendo aspecto visual (mío) de aspecto funcional (ux-criteria del Architect).** architecture.md define 21 DEMO-xxx. No todos son 100% visuales: DEMO-004 (routing), DEMO-006 (seed dinámico) son funcionales y los cubre ux-criteria.md (2ª invocación del Architect), aunque su MANIFESTACIÓN visual (shell, fotos visibles) sí tiene DC. Mapeé cada DEMO a sus DC y confirmé que los 21 tienen cobertura del aspecto visual antes de cerrar. Lección estable: el design-criteria cubre la CÁSCARA visual de cada DEMO; al verificar cobertura, separar "lo visual del DEMO" (mío) de "lo funcional" (ux-criteria) para no reclamar cobertura de lógica que no me toca ni dejar huérfano el aspecto visual de un DEMO funcional. Reportar el conteo de DC y BVC en el retorno le da al PM la métrica de completitud.

**Operativa — los archivos intermedios de los sub-designers también llegan truncados (ux-flows 2125 líneas, components 590); leer TODAS las páginas antes de consolidar o se pierden pantallas/componentes.** ux-flows.md llegó truncado a la línea 648 (de 2125) y components.md a 465 (de 590). Las pantallas de Tratamientos/Facturación/Reportes/Configuración (DC-046..049) y los componentes de dominio odontograma/tooth-state-selector (DC-076/077) viven después del corte. Leí las páginas faltantes con offset antes de escribir el consolidado. Lección estable (consistente con BA/orchestrator-plan/ux-flow): al consolidar, completar la lectura de CADA archivo intermedio con offset si llega "PARTIAL view"; un design-criteria basado en media lectura deja módulos y componentes sin DC. La verificación de existencia de los 3 archivos con Glob de ruta ABSOLUTA (el relativo falla, ya documentado 5+ veces).

### Feedback de: architect (2da invocación — ux-criteria)

**Patrón detectado — la sección CRM Tracking del template choca con proyectos que prohíben tracking**: El template de ux-criteria.md trae una sección "CRM Tracking" con un UX-090 obligatorio (eventos de open/navegación/scroll/heartbeat/CTA). Este proyecto PROHÍBE analytics/tracking (REQ-258/272, ADR-7, GAP-A01) y architecture.md ya había omitido deliberadamente el DEMO de tracking. Resolví manteniendo la sección como encabezado pero marcándola "N/A" con justificación trazable y convirtiéndola en un criterio de AUSENCIA para QA (verificar que NO existe SDK/evento), no de presencia. Además reutilicé el ID UX-090 para un criterio real ("Restablecer datos restaura el seed") y lo documenté para evitar confusión. Lección estable: cuando el template asume un capability que el proyecto prohíbe, no borrar la sección en silencio — dejarla explícita como N/A + criterio de ausencia, así QA tiene algo verificable y el PM ve la decisión. Coherencia con cómo el BA/architect manejaron el mismo conflicto aguas arriba.

**Patrón — alinear cada UX-xxx a un DEMO-xxx exige releer la tabla de Construcción Visual, no inferir de los REQ**: La tentación es derivar UX-xxx directo de los REQ funcionales (que están muy detallados), pero el campo "Origen" pide DEMO-xxx. Mapeé cada criterio al DEMO que lo origina y al final agregué una matriz inversa DEMO→UX para autoverificar cobertura de los 21 DEMO. Esto detectó que DEMO-018 (pantallas shell: reagendar, crear presupuesto, reportes detallados, sub-detalles, configuración) agrupa varios flujos que en los user journeys aparecen como first-class; tuve que asegurarme de que esos flujos tuvieran UX-xxx aunque en Construcción Visual sean "shell". Sugerencia: la matriz inversa DEMO→UX al cierre es barata y atrapa huecos que la lectura lineal no ve.

**Observación — ux-flows.md ya traía las decisiones de los gaps de UX resueltas**: El UX Flow Designer dejó §16 con 7 GAP-UX ya decididos (vista de calendario como query param, sub-nav de facturación como rutas hermanas, inicio en /reportes, notificaciones como panel, etc.). Los reflejé tal cual en los UX-xxx correspondientes y repetí los GAP-UX en mi tabla apuntando al UX-xxx que los resuelve, en vez de re-deliberar. Lección: cuando el documento de flujos upstream ya cerró decisiones de navegación, el ux-criteria las cristaliza en criterios testables y mantiene la trazabilidad GAP→UX, no las reabre.

**Patrón — el numbering del template (UX-001/020/040/060/080/090) es orientativo, no un cap**: Necesité más criterios por banda de los que el template ejemplifica (sobre todo en flujos e interacciones por la doble editabilidad pagos+odontograma + persistencia + cero-demo). Respeté las bandas semánticas del template (0xx navegación, 02x flujos, 04x estados, 06x mock data, 08x interacciones) pero numeré densamente dentro de cada banda (UX-080..094) y usé un sufijo (UX-023b) para insertar "reagendar" entre flujos sin renumerar. Funcionó para mantener legibilidad por sección. Sugerencia estable: tratar las bandas del template como namespaces temáticos y numerar libremente dentro, en vez de forzar el conteo a los ejemplos.

### Feedback de: plan-verifier
**Contexto**: Verificación Post-Planificación Fase 3 (paso 3d), proyecto Estudio Dental Mendieta (272 REQ + 20 NFR, NORMAL mode).

- **Patrón positivo (Architect)**: las 5 iteraciones citan rangos REQ-XXX explícitos y contiguos en sus tablas, lo que permitió una verificación programática 1:1 sin ambigüedad. Resultado: 272/272 sin huérfanos ni duplicados. Este formato de "rango explícito por iteración" es el ideal para verificación de cobertura y debería ser el estándar.
- **Patrón positivo (Design Orchestrator)**: los 29 BVC del cliente se transcribieron verbatim brief-criteria → visual-analysis → design-criteria, con columna de trazabilidad DC-xxx que NO altera el BVC. Excelente cadena de custodia de criterios prescriptivos.
- **Carencia menor (Architect 2da invocación / ux-criteria.md)**: la línea de auto-verificación de cobertura reporta "75 criterios UX-xxx" pero el cuerpo tiene 71 IDs únicos. El cálculo de los propios rangos citados (18+15+15+8+15) da 71. Recomendación: al cerrar un documento con sección de auto-conteo, sumar los rangos declarados antes de escribir el total, para evitar discrepancias resumen-cuerpo que generan ruido en verificación (aunque no afecten cobertura real).
- **Patrón a replicar (Architect/ux-criteria)**: la sección explícita "Cobertura de los 21 DEMO-xxx" mapeando cada DEMO a sus UX-xxx (y notando cuáles son visual-only → DC) hizo trivial confirmar 0 DEMO sin cobertura. Mantener.
- **Nota de organización**: los PNGs de referencia viven en `references/task-dasher/` (subcarpeta), no en `references/` raíz. La verificación contempló ambos casos; conviene que el contrato del Design Researcher documente la convención de subcarpeta por fuente para evitar falsos warnings de "references vacío".

### Feedback de: architect

**Patrón detectado — bump de versión de framework como ajuste quirúrgico, no rediseño**: El cliente observó que "Angular 17+" era una versión vieja para un proyecto nuevo en 2026. La tarea correcta fue un bump puntual (→ Angular 21) tocando SOLO el stack, ADR-1 (decisión + justificación + alternativas + Node), y dejar el resto intacto (módulos, modelo de datos, 5 iteraciones, 21 DEMO-xxx, dependencias). Verifiqué con grep que ninguna decisión arquitectónica dependiera de una API obsoleta: standalone, signals, control-flow `@if/@for/@defer`, lazy `loadChildren`, `@angular/build`/esbuild y el repositorio localStorage son todos vigentes (y mejores) en la última estable. Lección estable: ante "actualiza la versión X", el cambio es de stack+ADR+justificación, NO de plan; resistir la tentación de reabrir decisiones ya validadas.

**Patrón — distinguir referencias de versión reales de falsos positivos al grepear**: Al barrer "17" en el doc aparecían "17 mujeres" (conteo de fotos del seed), "17 gaps" (BA) y las menciones intencionales de "Angular 17" como versión DESCARTADA en la justificación/alternativas. Solo las del framework como versión vigente debían cambiar; las menciones de la versión vieja como contraste se conservan a propósito para dar trazabilidad al porqué del bump. Lección: tras un bump, dejar la versión anterior citada explícitamente como "alternativa descartada" documenta la decisión del cliente y evita que un futuro lector la reintroduzca por error.

**Nota de build-tooling version-agnostic**: El path de salida `dist/<app>/browser` y el `application` builder de `@angular/build` ya eran la forma vigente, así que el bump no exigió tocar la config de CI/CD ni el workflow de SWA — solo agregué el rango de Node soportado (`^20.19 || ^22.12 || >=24`) al stack y al ADR. Útil recordar: cuando el proyecto ya usa el builder moderno y rutas lazy idiomáticas, subir de major de Angular rara vez impacta la infra de deploy.

### Feedback de: devops

**Bloqueo encontrado (Fase 3.5) — cuota Free de Azure Static Web Apps agotada en la suscripción objetivo**: La suscripción `CEFSA-prod` ya tenía 10/10 SWAs Free (límite duro por suscripción). `az staticwebapp create --sku Free` falla con "too many static sites with SKU: Free". No lo resolví inventando: ni subí a Standard (tiene costo, el cliente pidió Free/sin costo) ni cambié de suscripción (el cliente fijó CEFSA-prod). Verifiqué read-only que `Microsoft Azure Sponsorship` y `FAYCA-prod` tienen 0/10 Free (capacidad libre) para ofrecer opciones concretas al PM. Lección estable: antes de provisionar un SWA Free, contar los existentes (`az staticwebapp list --query "length([?sku.name=='Free'])"`); si está cerca de 10, anticipar el bloqueo al PM con opciones (liberar slot / otra suscripción / Standard con costo) en vez de fallar tarde. Documentado en infrastructure-setup.md con los 3 caminos de desbloqueo.

**Patrón — todo lo NO bloqueado se completó igual para dejar el proyecto a un paso**: Aunque el SWA quedó bloqueado, el resto (git init, repo público en la org, .gitignore, staticwebapp.config.json con fallback SPA + headers, workflow de GitHub Actions, push, RG creado y vacío) se ejecutó completo. El push disparó el pipeline y verifiqué que el workflow es estructuralmente válido: falló exactamente en `deployment_token was not provided` (causa esperada por ausencia del SWA), no por error de sintaxis. Así, en cuanto el PM desbloquee, solo falta crear el SWA + cargar el token como secret. Lección: un BLOQUEADO parcial no debe paralizar el resto del setup; entregar todo lo controlable y dejar documentado el único paso faltante acelera el desbloqueo.

**Discrepancia de suscripción activa vs instruida**: La suscripción activa de az era `FAYCA-prod`, no la instruida `CEFSA-prod`. Hice `az account set` + `az account show` para confirmar antes de crear nada. Recomendación estable para DevOps: nunca asumir la suscripción activa; siempre fijarla y confirmarla por nombre antes de cualquier `create`.

**Artefactos de tooling colados en el commit inicial**: `git add -A` del primer commit incluyó `.playwright-mcp/` (36 snapshots de la fase de captura visual). Lo detecté revisando el desglose de paths staged por carpeta antes de commitear, lo agregué a `.gitignore` y lo saqué con `git rm --cached`. Confirmé además que las 29 fotos de `input/mockpeople/` SÍ quedaron versionadas (las necesita el runtime) y que `.DS_Store` quedó excluido. Lección: en el commit inicial, revisar siempre el set staged real (no confiar solo en .gitignore) porque no hay historial previo que haya filtrado caches de fases anteriores.

**Nota para el Developer (Fase 4) — alineación del output_location**: El workflow declara `output_location: "dist/estudio-dental-mendieta/browser"`, asumiendo que el proyecto Angular se llamará `estudio-dental-mendieta`. Si al hacer `ng new` se usa otro nombre, hay que ajustar ese valor en el workflow al `dist/<app>/browser` real, o el deploy subirá una carpeta vacía. También: el seed dinámico se genera vía hook `prebuild` en package.json (`node scripts/generate-manifest.mjs`) — Oryx corre `npm run build` y npm dispara el prebuild solo; no requiere paso extra en el workflow. Ambas notas quedaron en infrastructure-setup.md.

**Observación menor de CI**: GitHub anota que `actions/checkout@v4` y `actions/setup-node@v4` corren sobre Node 20, deprecado a partir de 2026-06-16 (forzarán Node 24). No bloquea hoy; conviene actualizarlas a versiones que soporten Node 24 en una pasada de mantenimiento del workflow.

### Feedback de: devops (2da invocación — Fase 3.5, provisión en Sponsorship tras decisión del cliente)

**Patrón crítico — "tiene cuota Free" NO implica "puedo provisionar ahí": separar capacidad de PERMISO/identidad antes de prometer un destino.** El cliente eligió `Microsoft Azure Sponsorship` porque mi reporte previo (read-only) decía "0/10 Free → hay cuota". La cuota era real, pero al intentar crear el RG fallé con `AuthorizationFailed` (la cuenta `hola@linkdesign.cr` es un invitado externo del tenant `asembiscr` con CERO asignaciones de rol en esa suscripción) y, al intentar el recurso, con `AADSTS50076` (MFA/Conditional Access exige un `az login --tenant ... --claims-challenge` interactivo, imposible en automatización). Lección estable y dolorosa: un conteo de cuota se obtiene con permiso de LECTURA, que un invitado suele tener en muchas suscripciones; eso NO predice permiso de ESCRITURA. Antes de ofrecer una suscripción alternativa al PM como "viable", verificar además escritura — p. ej. `az role assignment list --assignee <objId> --subscription <id>` (si da `[]`, no hay rol) o un probe real de `az group create`. De lo contrario el cliente toma una decisión sobre una opción que en realidad está bloqueada, y se pierde una ronda completa. En mi reporte previo listé Sponsorship y FAYCA-prod como "0/10 Free disponibles" sin haber comprobado escritura — ese es el gap a no repetir.

**Patrón — distinguir las DOS barreras de Azure y nombrarlas por separado al PM, porque tienen remedios distintos.** Aquí coincidieron (a) falta de rol RBAC (se arregla asignando Contributor) y (b) MFA/Conditional Access del tenant (se arregla con un `az login` interactivo con MFA, una vez). Son independientes: incluso con Contributor, el `AADSTS50076` seguiría apareciendo hasta hacer el login interactivo. Documenté ambas con su síntoma textual exacto y su remedio en infrastructure-setup.md, para que el cliente no asigne el rol y siga atascado por MFA sin entender por qué. Lección: ante un bloqueo de provisión en Azure, capturar el código de error literal (`AuthorizationFailed` vs `AADSTS5xxxx`) y mapear cada uno a su acción; "no tengo permiso" es ambiguo y puede esconder dos problemas.

**Patrón — la limpieza independiente del bloqueo SÍ se ejecuta; lo que depende del bloqueo, no.** Aunque la provisión quedó bloqueada, la tarea pedía borrar el RG huérfano `EstudioDentalMendieta-RG` en CEFSA-prod (vacío tras descartar esa suscripción). Eso NO depende de Sponsorship, así que lo completé: re-verifiqué vacío (`az resource list --query length` = 0) inmediatamente antes de borrar, borré con `--yes`, y confirmé con `az group exists` → `false`. En cambio NO creé el RG en Sponsorship, NO obtuve token, NO cargué el secret y NO usé el commit message "provision static web app..." que la tarea sugería, porque describiría algo que no ocurrió. Lección estable: ante un BLOQUEADO parcial, ejecutar lo que es independiente del bloqueo (housekeeping) y abstenerse de los pasos dependientes; y NUNCA usar un mensaje de commit que afirme un resultado no logrado — el commit debe describir lo realmente hecho (cleanup + doc del bloqueo), no la intención.

**Operativa — al cambiar de suscripción para verificar/limpiar en otras, restaurar siempre la suscripción activa a un default sano al terminar.** Toqué Sponsorship (set), FAYCA (per-command `--subscription`) y CEFSA (per-command para el delete), y dejé la activa restaurada en CEFSA-prod al final. Mantener la activa estable evita que la próxima corrida (mía o de otro agente que use `az`) opere sobre la suscripción equivocada por inercia. Consistente con la regla ya conocida de fijar+confirmar la suscripción.

### Feedback de: devops (3ra invocación — Fase 3.5, provisión final SWA Standard en CEFSA-prod tras autorización de costo)

**Patrón — cuando el desbloqueo es "el cliente autorizó el costo", la ruta correcta vuelve a la suscripción donde SÍ hay permiso de escritura, no a la de cuota Free.** Las dos invocaciones previas se bloquearon por (a) cuota Free llena en CEFSA-prod y (b) falta de rol+MFA en Sponsorship. La resolución del cliente (autorizar ~$9/mes de Standard) elimina AMBAS barreras de golpe: Standard no tiene el límite de 10, y al volver a CEFSA-prod recuperamos la escritura que la cuenta sí tiene ahí. Lección estable: el eje "permiso de escritura" pesa más que el eje "cuota Free" cuando el costo deja de ser restricción — provisionar donde la identidad ya funciona evita por completo el agujero de RBAC+MFA de la suscripción ajena. Confirmé `az account show` = CEFSA-prod antes de tocar nada (no asumí que seguía activa de la corrida anterior).

**Operativa — recrear un RG que una corrida previa borró es idempotente y correcto; verificar con `az group exists` antes.** El RG `EstudioDentalMendieta-RG` lo había borrado yo mismo en la 2ª invocación (era huérfano). Esta vez `az group exists` → `false` confirmó el estado, lo recreé limpio en `eastus2`, y ahí cayó el SWA. Lección: no temer recrear infra que uno mismo limpió antes — el ciclo borrar/recrear según la decisión vigente es normal; lo que importa es verificar el estado real (exists) en cada extremo, no fiarse de la memoria de la corrida anterior.

**Patrón de manejo de secreto — pasar el deployment token por archivo temporal con `umask 077`, nunca por variable inline en el comando ni imprimirlo.** Para cargar el token con `gh secret set` evité que el valor apareciera en el historial de comandos: lo escribí a `/tmp` con `umask 077` (solo el dueño lee), lo pasé con `--body "$(cat ...)"`, e inmediatamente `rm -f` el temporal. En los logs solo imprimí la LONGITUD del token (119 chars) como sanity-check, nunca el valor. Lección estable: el deployment token del SWA es una credencial; manejarlo como tal — fuera de logs, fuera del history persistente, archivo temporal de permisos restringidos borrado al terminar. `gh secret set` no emite output en éxito, así que la verificación es `gh secret list` (aparece el nombre + fecha), no el exit del set.

**Patrón — al provisionar el recurso, la respuesta de `create` puede traer campos nulos (`provisioningState: null`); confirmar con un `show` independiente en vez de fiarse del create.** El `az staticwebapp create` devolvió `provisioningState: null` (campo que solo se popula en consultas posteriores), pero el `defaultHostname` y `sku` ya venían correctos. No interpreté el null como fallo: corrí `az staticwebapp show` que confirmó SKU Standard + hostname + `repositoryUrl: null` (esto último valida que quedó standalone, sin GitHub linkado por Azure). Lección estable: separar "el create aceptó" de "el recurso existe como esperado" — un `show` post-create es la verificación canónica, y `repositoryUrl: null` es la señal concreta de que el SWA es standalone y el CI/CD lo controla mi workflow versionado, no la inyección de Azure.

**Operativa documental — al desbloquear, reescribir las secciones de "BLOQUEO ACTIVO" a estado resuelto en vez de dejarlas como histórico contradictorio.** infrastructure-setup.md tenía una sección 6 "⛔ Bloqueo activo" y una sección 2 "NO PROVISIONADO" de la corrida anterior. Las reescribí a estado real (provisionado, con valores reales) y condensé el historial del bloqueo a una nota breve de "cómo se llegó al destino final", para que Developer/QA/Plan-Verifier que leen el doc no encuentren dos verdades. Lección estable: el infrastructure-setup.md es un documento de ESTADO, no un log; cuando un bloqueo se resuelve, el estado nuevo reemplaza al bloqueo (con una línea de trazabilidad del porqué), no se acumula encima.

---

### Feedback de: ui-developer

**Bootstrap 5 selectivo en Angular: importar `_root.scss` reintroduce el azul `#0d6efd` y rompe la regla "ningún color Bootstrap" — importar SOLO functions/variables/maps/mixins/utilities + containers/grid + utilities/api, NUNCA `root`.** Mi primer intento usó los entrypoints self-contained `bootstrap-grid` y `bootstrap-utilities`; este último hace `@import "root"` internamente, que emite todas las `--bs-*` (incluido `--bs-blue: #0d6efd`, `--bs-primary: #0d6efd`, `--bs-link-color: #0d6efd`). Aunque mis overrides en theme.scss ganaban la cascada (declarados después, misma especificidad `:root`), los literales `#0d6efd` quedaban en el CSS y fallaban el check negativo de DC-029/BVC. Solución estable: crear un `bootstrap-subset.scss` que (a) redefine `$primary`/`$blue`/`$link-color` ANTES de importar, y (b) importa los parciales uno por uno OMITIENDO `root` y `reboot`. Resultado: 0 ocurrencias de `#0d6efd` y styles.css bajó de 136KB→90KB raw (7.46KB gzip). Como bonus, sin `root` el bundle es más chico. Para tokens `--bs-*` que sí necesito (font-family, radius), los defino yo en theme.scss.

**`@use` de Bootstrap parcial falla con "Undefined mixin _assert-ascending" — Bootstrap 5 espera `@import` (sus parciales dependen del estado global de @import).** Intentar `@use "bootstrap/scss/variables"` aislado rompe porque `_variables.scss` invoca mixins que solo existen en el árbol de `@import`. Lección: dentro del archivo subset, usar `@import` (Bootstrap 5 aún no es @use-compatible); las deprecation warnings de Sass por `@import` son esperadas y NO rompen el build de `@angular/build`. Encapsular todos los `@import` de Bootstrap en UN archivo subset dedicado y exponerlo al resto con `@use "styles/bootstrap-subset"` mantiene el resto del proyecto en `@use` moderno.

**Iconos Phosphor inline como componente: el innerHTML de SVG se sanitiza y desaparece — hay que usar `DomSanitizer.bypassSecurityTrustHtml` sobre el markup `<svg>…</svg>` completo, no path data suelto.** Angular strip-ea SVG children inyectados por `[innerHTML]`. Construir el string `<svg ...>${paths}</svg>` y pasarlo por `bypassSecurityTrustHtml` (son strings estáticos confiables del set Phosphor, no input de usuario) renderiza correctamente y respeta la CSP. Un componente `<app-icon name size>` con un mapa `Record<string,string>` de path-data resultó la forma más liviana (sin webfont, sin sprite externo, controla bundle y cumple `font-src 'self'`). Stroke óptico 1.5px a viewBox 256 = escalar `stroke-width = 1.5*256/size`.

**Angular 21 scaffold es ZONELESS por defecto (sin zone.js en node_modules ni polyfills en angular.json) — `app.config.ts` necesita `provideZonelessChangeDetection()` explícito.** `ng new` en Angular 21 no instala zone.js. Si no agrego el provider, el cambio de detección no funciona. Verificación: `ls node_modules/zone.js` (ausente) + `grep zone angular.json` (vacío) confirman zoneless. Con componentes OnPush + signals + control-flow nativo, zoneless funciona perfecto y es más performante.

**Deep-link a sub-rutas requiere que el nombre del proyecto Angular = el `output_location` del workflow SWA.** El workflow esperaba `dist/estudio-dental-mendieta/browser`, así que scaffoldeé con `ng new estudio-dental-mendieta`. Si el nombre no coincide, el deploy sube una carpeta vacía. Confirmé que `dist/<project-name>/browser` se genera con ese nombre exacto y contiene index.html + fonts + imagenes.

**Patrón de tab-content fuera del DOM (BVC-013) con router-outlet hijo: la ficha es ruta padre, cada tab es `loadComponent` hijo — el tab inactivo NO se renderiza (no es CSS display:none).** En vez de un `@if` con todos los tabs en el template, usé rutas hijas (`/pacientes/:id/odontograma`, etc.) con un `<router-outlet>` en la ficha. Esto garantiza que solo el tab activo está en el DOM, el tab se refleja en la URL (deep-link), y el botón atrás funciona — cumple BVC-013/DC-073 estructuralmente, no por ocultamiento visual. Los tab-components leen el `:id` del padre vía `route.parent.snapshot.paramMap`.

**Verificación visual sin Playwright/Puppeteer instalados: Google Chrome headless (`--headless --screenshot --virtual-time-budget`) es suficiente para validar la cáscara.** Capturé login, dashboard, lista, ficha, odontograma, agenda, mobile (390px) con `"/Applications/Google Chrome.app/Contents/MacOS/Google Chrome" --headless --window-size=W,H --screenshot=out.png --virtual-time-budget=4500 URL`. Para `@defer (on viewport)`, el contenido diferido solo aparece si el elemento entra en el viewport — usar una ventana alta (1400px) para que el odontograma cargue en la captura. El overlay de error del dev-server puede aparecer en una captura si se toma mientras el HMR recompila un edit; reiniciar el serve + limpiar `.angular/cache` y recapturar confirma si el error es real o stale (en mi caso era stale: `ng build` y `tsc --noEmit` pasaban limpios).

### Feedback de: plan-verifier
**Contexto**: 4a-verify (cáscara visual vs DC-xxx), Estudio Dental Mendieta, modo NORMAL.

- **Patrón de gap recurrente en cáscaras con set de íconos inline propio**: cuando el UI Developer arma un set Phosphor inline (map nombre→path) con fallback silencioso a un glifo "question", es fácil que queden nombres referenciados pero no definidos. El build NO falla (el fallback evita el crash), pero el usuario ve "?" en pantallas reales. **Recomendación para el UI Developer**: agregar un test/assert que valide que TODO `name` usado en templates + en data tiene entrada en el map de íconos, o eliminar el fallback silencioso en dev. En este proyecto faltaron `currency-circle-dollar` (costo/pago, en 3 componentes) y `sparkle` (limpieza/blanqueamiento, en data).
- **Técnica de verificación efectiva**: extraer con grep todos los `name="..."`/`[name]="'...'"`/`icono: '...'` y diff contra los keys definidos del set. Detecta el gap de íconos en segundos sin levantar el browser.
- **Lo que salió excelente y conviene reforzar como referencia para futuros proyectos**: (1) tabs de ficha implementados como rutas hijas con router-outlet → garantiza DC-073/BVC-013 "contenido inactivo NO en DOM" por construcción, no por CSS display:none; (2) Bootstrap importado sin la capa `root` en `bootstrap-subset.scss` → elimina de raíz el `#0d6efd` (DC-029) en vez de sobrescribirlo; (3) GAP-A04 resuelto consistentemente (botón primary = texto oscuro sobre azul medio). Estos 3 patrones son verificables y robustos; vale citarlos como buenas prácticas.
- **BVC-027 (cero rastro demo) y "cero analytics/CRM" se verifican mejor con un barrido grep negativo de todo src/** filtrando comentarios y atributos `placeholder=` de inputs / sintaxis `@placeholder` de `@defer`, para evitar falsos positivos.

### Feedback de: ui-developer

**Patrón detectado — la verificación de "iconos definidos vs referenciados" necesita cruzar TODAS las formas de referencia, no solo `<app-icon name="...">`**: El gap GAP-1 (íconos `currency-circle-dollar` y `sparkle` resolviendo al fallback "?") existía porque el set en `icon.component.ts` se quedó corto frente a los nombres usados. La causa raíz: los nombres de ícono no se referencian solo como atributo literal `name="x"` en templates; también llegan vía (a) propiedad de datos en `placeholder.data.ts` con clave en español `icono: 'x'`, y (b) ternarios dinámicos `[name]="cond ? 'a' : 'b'"`. Un grep solo de `name="..."` se pierde la mitad. Lección estable: al definir/auditar un set de íconos inline, hacer un cross-check programático que (1) extraiga las claves DEFINIDAS del Record, (2) recolecte TODOS los nombres referenciados cubriendo `app-icon name="..."`, claves de datos (`icon:`/`icono:`/`glyph:`), y literales dentro de ternarios `[name]`, y (3) reporte el conjunto `referenciados - definidos`. Correrlo como auto-verificación ANTES de cerrar la cáscara evita que el fallback silencioso "?" llegue a QA.

**Cuidado con falsos positivos al regexear ternarios de ícono**: mi cross-check inicial marcó `pago` y `radiografia` como "missing", pero eran valores de COMPARACIÓN (`m.signo === 'pago'`, `d.tipo === 'radiografia'`), no nombres de ícono — el regex de ternario capturó el literal de la condición además del literal del ícono real. Lección: tras el cross-check automático, inspeccionar cada candidato faltante en su línea real antes de actuar; un literal dentro de un `[name]` puede ser el operando de un `===`, no el ícono. El subconjunto realmente faltante coincidió exactamente con lo que QA reportó (2 íconos).

**Resolución preferida cuando el glifo intencionado existe en la librería oficial**: ambos íconos (`currency-circle-dollar`, `sparkle`) son glifos reales de Phosphor "regular". Agregué el path-data oficial al set en vez de reasignar a un glifo aproximado existente, porque preserva el significado exacto (dólar-en-círculo = costo/pago; destello = limpieza/blanqueamiento/profilaxis) y mantiene la coherencia óptica del set (mismo viewBox 256, stroke fijado por el componente). Reasignar a `money`/`tooth` habría pasado el build igual pero degradado la intención de diseño (DC-027/044/061/062). Regla: si el nombre referenciado es un glifo canónico del set elegido, traer el path oficial; reasignar solo si el nombre no existe en la librería.

### Feedback de: plan-verifier
**Contexto**: 4a-reverify (cáscara visual, modo NORMAL) — cierre de gap de íconos en `icon.component.ts`.
- **Patrón a reforzar (UI Developer / target del feedback)**: el gap original (íconos `currency-circle-dollar`/`sparkle` sin definir → fallback "?") tuvo su raíz en que los icon-keys NO siempre vienen de `<app-icon name="literal">` en templates; un porcentaje importante llega vía bindings dinámicos `[name]="x.icono"` alimentados desde `core/data/placeholder.data.ts` (campo `icono:`) y desde nav-data (`icon:`) en componentes de feature, además de ternarios inline y mappers como `toast.glyph()`. El cross-check de íconos debe abarcar TODAS estas fuentes antes de cerrar, no solo los `name=` literales.
- **Técnica de verificación efectiva (para mi propio rol)**: el cross-check definitivo es un diff de conjuntos `referenciados − definidos` extrayendo claves de (1) `name="..."` literal, (2) valores de los campos de datos que alimentan `[name]` (`icono:`/`icon:`), (3) literales dentro de ternarios `[name]="cond ? 'a' : 'b'"`, (4) funciones que retornan icon-keys. Complementarlo con `grep` de las claves en el bundle JS emitido confirma que resuelven a su propio path-data y no al fallback. Esta receta debería aplicarse en todo proyecto con un componente de íconos inline tipo set.
- **Confirmado OK**: el fix fue puramente aditivo (2 entradas a `RAW`), build de producción verde, sin regresión del resto de los DC-xxx ya aprobados.

### Feedback de: developer
**Contexto**: Fase Construcción Visual paso 4b — agregar funcionalidad de la DEMO sobre la cáscara visual del UI Developer (Estudio Dental Mendieta, frontend-only, localStorage). Modo NORMAL.

**Patrón estable y de altísimo apalancamiento — reconectar la cáscara al store cambiando SOLO el origen de datos de la clase, nunca el template.** El UI Developer dejó cada pantalla consumiendo constantes `PLACEHOLDER_*` importadas directamente (arrays estáticos) con datos argentinos ricos. La forma correcta de "agregar funcionalidad sin tocar estilos" fue: (1) crear un `StoreService` con signals cuyos SELECTORES devuelven EXACTAMENTE los shapes de presentación que los templates ya esperan (`Patient`, `Appointment`, `Budget`...), (2) en cada componente, reemplazar `protected readonly x = PLACEHOLDER_X` por `protected readonly x = this.store.xSignal` o `computed(() => this.store.metodo(id))`, (3) tocar el template solo cuando un campo plano pasó a signal (`x` → `x()`). Resultado: 0 cambios visuales, toda la pantalla se volvió reactiva y persistente. Lección: cuando el contrato de datos (interfaces de presentación) ya existe, hacer que el store PRODUZCA ese contrato en vez de cambiar los consumidores — minimiza el diff y respeta la frontera "no tocar estilos del UI Developer".

**Patrón — `patient-context.ts` (helper compartido por los 6 tabs) fue el punto de máximo apalancamiento.** Los tabs de la ficha resolvían el paciente vía una función `currentPatient()` que importaba el array placeholder. Reescribir SOLO esa función para que lea `inject(StoreService).patientById(id)` reconectó los 6 tabs sin tocar ninguno. Lección estable: buscar los helpers/funciones compartidas que muchos componentes usan para leer datos; reconectar el helper propaga el cambio a todos sus consumidores de una.

**Patrón crítico de flujos multi-paso — el estado del wizard NO puede vivir en el componente porque cada paso es una RUTA dedicada que re-instancia el componente.** En multi-pantalla estricto (ADR-5), `/agenda/nuevo/paciente` → `/profesional` → `/confirmar` son 3 rutas; navegar entre pasos DESTRUYE y RE-CREA el componente, perdiendo cualquier `signal` local. La solución es un servicio de flujo `providedIn: 'root'` (`AppointmentFlowService`, `BudgetFlowService`) que retiene la selección entre pasos y permite el pre-relleno desde la ficha ("Agendar turno" preselecciona el paciente y entra al paso 2). El componente solo lee/escribe el servicio. Lección estable: si un flujo tiene una ruta por paso, su estado va en un servicio scoped; reset al iniciar el flujo y tras confirmar. Esto también hace que el botón "Atrás" del navegador preserve datos (UX-015) gratis.

**Patrón — APP_INITIALIZER (`provideAppInitializer`) + un `canActivate` guard cubren el deep-linking de forma robusta y redundante.** El store se hidrata (localStorage → seed determinista) ANTES del primer render vía `provideAppInitializer(() => inject(StoreService).hydrate())`; además un `seedReadyGuard` en la ruta del shell espera la hidratación si por algún motivo aún no terminó. Con ambos, entrar directo a `/pacientes/pac-007/pagos` (deep-link, refresh, link compartido) siempre encuentra el estado listo. Lección estable: en una SPA con estado que debe existir antes de cualquier vista, el initializer es la vía principal y el guard la red de seguridad; `hydrate()` debe ser idempotente (`if (this._state()) return`).

**Patrón estable — seed dinámico determinista: el manifest se materializa en build-time y el generador usa PRNG de semilla fija; verificar volúmenes Y determinismo transpilando con el esbuild que ya trae @angular/build.** El `scripts/generate-manifest.mjs` (hook `prebuild`) recorre `input/mockpeople/{hombres,mujeres}/`, deriva género (carpeta) + edad (nombre, con soporte de sufijo `23-1.jpg`), copia fotos a `public/imagenes/...` y escribe `public/seed/manifest.json`; runtime hace `fetch('seed/manifest.json')`. El generador (`seed.generator.ts`) deriva 14 entidades con un `mulberry32` de semilla fija → mismo manifest = mismo estado, por lo que "Restablecer datos" devuelve el estado original idéntico. Para verificar sin levantar el browser, transpilé el generador con `esbuild` (disponible en node_modules vía @angular/build) y corrí `generateSeed(manifest)` en node, comparando dos corridas (`JSON.stringify` igual) y los conteos contra los mínimos. Detecté así que cumplía 29 pacientes / 56 turnos / 49 tratamientos / 22 presupuestos / 28 facturas / 44 documentos y que el reset es fiel — todo antes de tocar el build de Angular. Lección estable: para lógica pura (seed/generadores/store sin deps de Angular), transpilar con el esbuild local + correr en node es un test de humo rapidísimo que valida volúmenes, distribuciones, coherencia (saldo→estadoCuenta) y determinismo sin overhead de browser ni framework. IMPORTANTE: correr el script desde el cwd del proyecto (no /tmp) para que node resuelva `esbuild`.

**Patrón — el estado de cuenta DERIVADO (computed del saldo) es lo que garantiza coherencia entre el badge de la lista y el tab Pagos (UX-085); nunca duplicar el estado.** El `estadoCuenta` (al-día/con-deuda/vencido) NO se almacena: se calcula de los `AccountMovement` (cargos − pagos, + antigüedad para "vencido"). Cuando `registrarPago()` agrega un movimiento de pago, el saldo recalcula y TODOS los lugares que lo muestran (badge en `/pacientes`, resumen del tab Pagos) se actualizan solos porque leen el mismo computed. Verifiqué que un pago que salda la deuda mueve el paciente de con-deuda/vencido → al-día. Lección estable: cuando un valor aparece en varias pantallas y debe ser coherente tras una mutación, derivarlo con `computed()` desde la fuente única, no copiarlo en el modelo; la coherencia es automática y no hay que sincronizar.

**Operativa confirmada (consistente con todo el equipo) — el build de producción es el gate real, no el de desarrollo.** El dev build (unminified) reportó "Initial total 1.56 MB" que asusta, pero el de PRODUCCIÓN reportó 424.50 kB raw / 102.37 kB gzipped — holgadamente bajo el budget de 500KB gzipped (sin warning ni error). El budget de `angular.json` solo se evalúa en la config production. Lección estable: para validar el NFR de bundle, correr `ng build --configuration production`; el tamaño de dev no refleja el gzipped final ni dispara el budget. Además `tsc --noEmit -p tsconfig.app.json` (con `strictTemplates: true`) es un gate complementario que confirma que TODOS los bindings de template resuelven, útil cuando no hay browser headless disponible para smoke-test runtime.

**Cuidado — al convertir un campo de array a signal, el template rompe en silencio hasta el build con `strictTemplates`.** Varios componentes tenían `professionals = PROFESSIONALS` (array) usado como `@for (p of professionals; ...)`. Al pasar a `professionals = this.store.professionals` (signal), el template debe usar `professionals()`. `strictTemplates` lo detecta como `TS2488: must have a '[Symbol.iterator]()'`. Lección: al migrar arrays a signals, hacer grep del símbolo en el template (`@for (x of NOMBRE`, `NOMBRE.length`, `[input]="NOMBRE"`) y añadir `()` en cada uso; el error de iterador del compilador es la pista exacta de cuáles faltaron.

**Frontera respetada — NO agregué inputs a componentes visuales del UI Developer; cuando necesité un comportamiento que el componente no exponía, usé la salida que ya existía.** Quise un estado `busy` en el `confirm-dialog` para el reset, pero ese componente es del UI Developer y no lo expone; en vez de añadir un input (tocar su API/estructura), quité el binding y dejé el reset síncrono (es rápido). La única excepción que me permití en un componente compartido fue un `effect` puramente FUNCIONAL en `data-table` que reencuadra la página cuando el set de filas se achica tras filtrar (bug real: página vacía) — no cambia nada visual, solo evita un estado roto. Lección estable: respetar "no tocar componentes visuales" significa preferir las salidas/inputs existentes; si hay que tocar un componente compartido, que sea lógica no-visual que corrige un bug, documentada como tal, nunca un cambio de su contrato visual.

**Patrón — el manifest en path anidado (`/seed/manifest.json`) puede no quedar excluido del SPA-fallback de SWA; el ManifestLoader debe tener fallback robusto en vez de depender de la config de DevOps.** El `staticwebapp.config.json` (de DevOps) excluye `/*.{...json...}` del navigationFallback, pero ese glob suele ser root-only y podría no cubrir `/seed/manifest.json` en producción (devolviendo index.html → `JSON.parse` falla). En vez de pedir cambiar la config de DevOps, el `ManifestLoader` valida `data?.pacientes?.length` y cae a un `FALLBACK_MANIFEST` con las mismas 29 entradas si el fetch no trae JSON válido. Así la app nunca queda sin pacientes, sea cual sea el comportamiento del fallback de SWA. Lección estable: cuando un asset JSON crítico se sirve en hosting estático con SPA-fallback, el loader debe ser defensivo (validar forma + fallback determinista) y no asumir que la config de routing lo excluye correctamente; mantiene la robustez sin invadir archivos de otro rol.

---

### Feedback de: plan-verifier (4b-verify)

**Patrón útil — el grep de "ausencia" requiere regex preciso para evitar falsos positivos masivos**: Al verificar DEMO-021 (cero tracking) y la prohibición de ARCA/AFIP, un primer grep amplio (`analytics|track|afip|arca`) devolvió docenas de falsos positivos por substrings dentro de palabras legítimas: `track`→"`attrack`"/`fechaLarga`, `ga(`→`calendar-blank`, `arca`→"m**arca**r notificación", "Arc**a**da superior", "Av. Las Her**a**s C**a**rc**a**s", "v**aria**bles de m**arca**". Lección estable para verificación de ausencia: (1) usar regex anclado/específico (`dataLayer|gtag\(|\.track\(|sendBeacon|mixpanel|Sentry\.`), (2) separar el grep de strings de usuario (filtrando comentarios `//`,`/**`) del grep de código, (3) tratar las menciones en comentarios `/** DEMO-xxx */` y "SIN ARCA/AFIP" como confirmación de cumplimiento, no como violación. La prohibición correctamente escrita en un comentario es evidencia POSITIVA.

**Patrón útil — verificar el seed dinámico por código + build + runtime, no solo por código**: Para DEMO-006 (seed desde fotos con volúmenes mínimos) la verificación más sólida fue triple: (a) leer los generadores y confirmar las garantías de volumen (`while (x.length < N)`), (b) correr `npm run build` y confirmar que `manifest.json` y las 29 fotos quedan en `dist/.../seed` y `dist/.../imagenes`, (c) servir el build y hacer `curl` a `/seed/manifest.json` (conteo géneros) y a una foto (HTTP 200 + bytes). El paso (c) detecta desajustes de path que el código solo no revela (ej. el manifest usa `/imagenes/...` no `assets/seed/...` como decía architecture.md — funciona, pero solo se confirma sirviendo). Para frontend-only con seed build-time, esta verificación en runtime del artefacto servido debería ser estándar.

**Patrón útil — deep-linking se verifica con curl al build servido con fallback SPA**: DEMO-004 exige que `GET /pacientes/pac-007/pagos` renderice (no 404). Servir el `dist` con un static server con fallback a index y hacer `curl -o /dev/null -w "%{http_code}"` a una ruta interna profunda confirma el comportamiento de SWA `staticwebapp.config.json` sin necesidad de navegador. Barato y determinante.

**Observación de alcance — distinguir "shell aceptable" (DEMO-018) de gap real**: El prompt aclaró explícitamente que las pantallas shell navegables NO son gap. Verifiqué cada componente de DEMO-018 por (a) line-count (descartar placeholders triviales) y (b) grep de "en construcción/TODO/por implementar" (vacío). Varias pantallas "shell" estaban de hecho funcionales (reagendar persiste, reportes calculan gráficos). Lección: cuando el alcance marca ciertos entregables como "shell aceptable", la verificación debe distinguir "navegable con layout" (cumple) de "página en blanco/placeholder de error" (gap), y NO penalizar funcionalidad que excede el shell (es scope creep benigno, solo se reporta).

**Carencia detectada en upstream — input opcional no provisto degrada elegantemente pero conviene anotarlo**: El pool de imágenes de documentos (`public/imagenes/documentos`) no se entregó, dejando `manifest.documentos: []`. El código lo maneja con placeholder controlado (UX-048), así que NO es gap de DEMO, pero lo dejé como observación porque un verificador podría confundir "thumbnails con placeholder" con "tab de documentos roto". Lección: cuando un input opcional falta y el código degrada bien, reportarlo como observación explícita evita que QA (4f) lo malinterprete como defecto visual.

### Feedback de: developer (simplify — Construcción Visual)

**Patrón de duplicación encontrado y corregido — helpers de formato de fecha.** El formateo de fechas es-AR (arrays `dias`/`meses` + parseo ISO `split('-').map(Number)`) estaba duplicado literalmente en 3 componentes de Agenda (`appointment-create.fechaLabel`, `reschedule.fechaActual` — idénticos byte a byte; `appointment-detail.fechaLarga` — variante larga) y existía además como funciones privadas en el store (`formatShort`/`formatDmy`/`parseIso`/`DIAS`/`MESES`). Consolidado en `src/app/shared/date-format.ts` (puro, sin deps Angular → no rompe la verificación esbuild+node del store): `parseIsoDate`, `formatShortDate` ("Mié 4 jun"), `formatLongDate` ("Miércoles 4 de junio de 2026"), `formatDmyDate` ("04/06/2026"). Resultado: −3 cuerpos de función duplicados, −5 arrays de meses/días repetidos; bundle sin cambio relevante (424.5→424.9 kB raw, tree-shaken en chunks lazy).

**Lección sobre qué NO refactorizar (evitar over-engineering):**
- Los wizards `appointment-create` y `budget-create` comparten estructura (`next/back/STEP_ROUTES/confirmCancel`) pero con distinto número de pasos y validaciones; extraer una base compartida acopla dos flujos independientes por poco beneficio → se dejó como paralelismo legítimo.
- El cast `(e.target as HTMLInputElement).value` aparece 13× en 7 archivos, pero es idioma Angular de 1 línea; envolverlo en un helper añade un import en cada archivo (churn lateral) sin ganancia neta de mantenibilidad → se dejó.
- Los arrays `weekdays` (lunes-primero, para el header del calendario) NO son los mismos que `DIAS` del store (domingo-primero, para indexar por `getDay()`): semánticas distintas, no consolidar.

**Confirmaciones de higiene:** sin TODO/FIXME/HACK; sin restos de CRM/analytics/PLACEHOLDER_* ni lenguaje de demo; todos los componentes shared tienen ≥1 uso (sin componentes muertos); todos los exports de `models.ts`/`status-map.ts`/`ar-data.ts` están referenciados (los "unused" de `Clinic`/`ContactoEmergencia` eran falsos positivos — se usan como tipos de propiedad dentro de `models.ts`). Métodos largos del store (`kpis`) y de `report-detail` (`chartsX`) ya están bien descompuestos: una responsabilidad por unidad, lectura lineal — no requieren partición.

### Feedback de: developer
**Contexto**: MODO Security Review (fallback paso 4d, Construcción Visual) sobre app Angular 21 frontend-only (localStorage, sin backend, Azure SWA).

**Lecciones / patrones**:
- **bypassSecurityTrustHtml en componentes de icono inline**: el patrón "SVG inline desde diccionario de paths" obliga a bypassear el sanitizer (Angular descarta sub-elementos SVG). No es vulnerable mientras el path data salga SOLO de un allowlist interno y el `name` crudo NUNCA se interpole en el markup. Riesgo residual real: valores numéricos (ej. `size`) tipados en TS pero borrados en runtime → conviene coercerlos a entero acotado antes de interpolarlos en el markup bypasseado. Es el único valor no-allowlist que llega al HTML.
- **CSP de un static frontend-only**: `style-src 'unsafe-inline'` es OBLIGATORIO (Angular emite inline styles vía `[style.x]` y component `styles`), no se puede quitar sin romper visuales. Lo importante es que `script-src` quede `'self'` sin `unsafe-inline/eval`. Hardening sin impacto conductual: agregar `frame-src 'none'` y `form-action 'self'`.
- **Validación de inputs en viewcases**: los formularios (pago, crear/editar paciente) ya validaban en boundaries (monto >0 / !isNaN, DNI 7-8 dígitos, requeridos, `.trim()`, fechas regex-parseadas). En frontend-only sin backend, esta validación de UI es la única capa — está bien que viva en los componentes/signals.
- **Secrets**: confirmado patrón correcto — deployment token SOLO en `secrets.AZURE_STATIC_WEB_APPS_API_TOKEN`, `.gitignore` excluye `.env*`, sin `.env` en repo. El `value="········"` (middots U+00B7) en el input password del login es decoración visual, NO un secreto.
