# Dependencies
node_modules/

# Build output
dist/
/tmp/
/out-tsc/
/bazel-out/

# Angular / build caches
.angular/
.nx/

# Test coverage
coverage/

# Environment / secrets (NEVER commit)
.env
.env.*
!.env.example

# IDE / editor
.idea/
.vscode/*
!.vscode/extensions.json
*.sublime-workspace

# System files
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Misc
*.tsbuildinfo

# Playwright MCP capture artifacts (transient tooling output from visual research)
.playwright-mcp/

# NOTE: input/mockpeople/ (patient photos) and, once Phase 4 creates it,
# public/ (where the photos are copied for runtime) are intentionally
# NOT ignored — the static app needs them at build/deploy time.
